# H.I.D.E 后续开发规划

> 定位：**轻量文本编辑器**。每项功能都要过"轻量"这道门槛：不显著增加包体积、不引入常驻后台、不让界面复杂化。
> 当前版本：v1.4.2（2026-09-23 发布：安卓外部「打开方式 / 分享」修复、两端「获取另一版」互引面板、
> 更新链国内镜像与多源回退、触屏取色入口 + 全编辑器通用选区工具条、手机顶栏脏点配色勘正）

## 版本策略

应用已完成正式发布，此后走 SemVer 常规节奏：**功能版挂主题、补丁版随修随发**。
每个版本只挂一个主题、功能项控制在三项以内，做完再规划下一版 —— 避免一次性铺太多摊子。

**v1.5 是对「≤3 项」的一次有意例外**（2026-09-23 定）：主题仍是设备互联，另挂 51（语言高亮）与
52②（弹窗层 48dp）两项收尾。理由与代价都写清楚：52② 搭车是因为互联要新写一批手机/平板界面，
48dp 实测那一轮本来就要做，拆开等于再上一遍设备；51 与主题无关，纯粹是它塞进补丁版会把发版拖长，
既然 v1.5 周期本来就长就一起走。**代价：本版发版周期显著长于以往，且中途不出补丁版**。
因此内部按阶段拆两次发布（见 v1.5 小节的「发布拆分」），不要把七件事攒到最后一个 tag。

历史里程碑：0.1.0 初始提交、0.2.0 安卓移植、0.3.0「及格线」批次、0.4.0 编辑体验、
0.5.0 差异化小功能、v1.0.0 正式发布（发布链路 + 工程化）、v1.1.0 可视化内容视图、
v1.2.0 大文件与跨文件检索、v1.3.0 结构化内容与新格式、v1.4.0 安卓全面触屏适配、
v1.4.1 触屏收尾补丁、v1.4.2 外部打开与更新链镜像。

## 现状盘点（v1.4.1，2026-09-21 复核）

代码规模：前端 TS/TSX 190 文件 / 39,823 行，Rust 3,353 行；测试 71 文件 / 855 例全绿（实测 `npx vitest run`）。

已有能力：30+ 语言语法高亮、小地图、粘性滚动、代码折叠、
多标签页（脏状态；桌面支持标签拖拽重排 / 拖出脱离成窗 / 跨窗合并 / 按窗会话恢复）、
查找/替换/跳转到行、Markdown 分屏预览（公式 / Mermaid / 表格编辑 / 导出 HTML 带代码高亮 / 网址导入）、
大纲导航浮窗与图片资产管理（粘贴落盘 / 远程图源本地化）、CSV 网格编辑器（排序 / 筛选）、
JSON/YAML 结构树与分屏视图、十六进制查看、打印/导出 PDF、
SVG 工作台（预览 + 可视化编辑：点选 / 拖拽 / 调色板换色 / PNG 导出）、图片查看器、
文件树侧栏（递归监视自动刷新）、外部修改 Diff 时间线、会话恢复与草稿、超大文件分块预览、
跨文件搜索、编码检测与转换（8 种）、换行符保留与转换、自动更新、文件关联与单实例。

安卓端（v1.4.0 起从"能看"到"能用"）：全面触屏适配（长按菜单替代右键 / hover，触点 ≥44dp、
关键操作 48dp，双指捏合缩放接图片查看器、SVG 画布与工作台，键盘避让，物理键盘探测）、
手机端覆盖式文件树抽屉 + 顶栏入口（图片查看入口随之回归）、SAF 文件管理
（树内新建 / 重命名 / 删除，经 Kotlin 桥 `createInTree` / `renameEntry` / `deleteEntry`）、
平板推拉式文件树与菜单栏常驻图标钮、系统分享（`ACTION_SEND`）、Markdown 选区工具条、
release 独立签名（与 v1.3.x debug 包不同签名，跨边界升级需卸载重装一次）。
v1.4.1 再补两处：表格行列增删的触屏工具条抬到 ≥44dp 并钉在键盘之上、SVG 检查面板补位移
X/Y 输入框与 ± 步进（触屏没有方向键，微调此前只能靠拖拽）。

仍存在的边界（后文按此排期）：

| 类别 | 边界 |
| --- | --- |
| 平台 | 只有 Windows（NSIS）与 Android（侧载 APK）；应用内更新仅桌面。**不补 macOS / Linux 已写进 README**（依据见 v1.4.1 条目 40） |
| 大文件 | 编辑上限 200 万字符（`tabModel.ts:70`，之上只读降级）；查找装饰高亮仍限 5000 条（`editorSearch.ts:20`） |
| 安卓 | 无外部修改 diff 与文件监听（`App.tsx:198` 的 `enabled: isTauri && !IS_ANDROID_APP`；SAF 不提供递归 watch，`safDirLister` 因此不实现 `watch`，侧栏退化为手动刷新）。v1.4.1 条目 41 收口后**仍存**的触屏边界：行号槽交互在触屏关闭（已移出暂挂，见下）、源码行内取色圆点 11.9dp 难命中（条目 41 末记） |
| 工程 | 无 lint / format；`App.tsx`（2,796 行，全仓最大）仍无测试；19 个 hooks 只有 3 个有测试（`useEditorState` / `useLongPress` / `usePinchZoom`）；`csp: null`；fs 权限 `path: "**"` |
| 互联 | 桌面与安卓两台设备之间无任何通道，文件要靠系统分享或手动搬运 —— v1.5 的全部内容 |

---

## v0.3 —— 补齐文本编辑器的"及格线"（P0）✅ 已完成（2026-09）

1. ✅ **查找 / 替换 / 跳转到行**（最高优先级）
   - 自绘 FindReplaceBar（`src/components/FindReplaceBar.tsx`）+ 纯函数查找引擎（`src/lib/searchCore.ts`）+ 高亮扩展（`src/lib/editorSearch.ts`），不依赖 @codemirror/search 面板，桌面 / 手机 / 平板同一套交互
   - 正则（含 `$1` 引用替换）、大小写、全词匹配、匹配计数与循环导航；`Ctrl+F` / `Ctrl+H` / `Ctrl+G`
2. ✅ **编码支持（中文用户刚需）**
   - Rust 侧 `encoding_rs`（`src-tauri/src/encoding.rs` + `read_text_file` / `write_text_file` 命令）：BOM → 二进制判定 → 严格 UTF-8 → GBK / GB18030 / Big5 / Shift_JIS 无损认定 → GBK lossy 回退，含 12 个单元测试
   - 保存按原编码写回（BOM 保留）；状态栏「以编码重新打开」/「转换编码并保存」
   - 安卓 SAF 经 Kotlin 桥 `writeUri(uri, content, encoding, bom)` 按编码写盘；浏览器端 JS 启发式检测（`src/lib/encoding.ts`）
3. ✅ **换行符处理**：打开保留原始 CRLF/LF/CR（编辑器内 LF 归一，保存时还原），状态栏显示并一键转换，eol 纳入脏状态判定
4. ✅ **状态栏完善**：`行:列`、选中字符数、编码（含 BOM 标记）、换行符
5. ✅ **最近打开文件**：最多 15 条（`src/lib/recentFiles.ts`），菜单显示最近 8 条，打开 / 另存 / 拖拽均记录

> v0.3 后续细化：查找浮层统一 portal 弹出在指针位置（不跟随）；新增 Markdown 纯预览态查找
> （`PreviewFindBar`，只搜渲染后文本，CSS Custom Highlight API 高亮，旧 WebView 降级为计数 + 滚动定位），
> 替代此前 WebView/浏览器自带的查找栏。

## v0.4 —— 编辑体验（P1）✅ 已完成（2026-09）

6. ✅ **设置面板**（`src/components/SettingsDialog.tsx` + `src/lib/settings.ts` 统一存取，逐字段校验回退）
   - 字体、字号、行高、Tab 宽度、空格/Tab 缩进、自动换行（仅 Markdown / 总是 / 从不）、显示空白符
   - 小地图 / 粘性滚动开关（手机与降级模式下自动不可用）
7. ✅ **自动保存与草稿恢复**
   - 自动保存（默认关）：按间隔把「有路径且脏」的文件静默落盘；确认弹窗/退出流程期间暂停，避免和用户「不保存」的决策打架
   - 草稿恢复（始终开启）：脏标签内容防抖写入 `lib/drafts`（localStorage），会话恢复时叠加并标脏；
     「关闭不保存 / 退出不保存」明确清除草稿；单条草稿超 2MB 放弃保护（localStorage 配额）
8. ✅ **快捷键补齐**：`Ctrl+W` 关标签、`Ctrl+Tab` / `Ctrl+Shift+Tab` 切换标签；「键盘快捷键」帮助弹窗（桌面菜单 + 手机顶栏）
9. ✅ **性能与体积**
   - 13 个语言包全部改为按需 `import()`：主 bundle 2251 KB → 1667 KB（gzip 759 → 546 KB，-26%）
   - 大文件保护：超 200 万字符自动关闭语法高亮/小地图/补全/选区匹配，状态栏显示「大文件」
   - 二进制文件（检测含 NUL）：只读预览，状态栏显示「二进制 · 只读」

> v0.4 后续细化：标签栏右键菜单（新建 / 关闭其他 / 关闭全部，脏标签逐个确认）。

## v0.5 —— 小而美的差异化（P2）✅ 已完成（2026-09）

10. ✅ **可选文件树侧栏**（`FileTreeSidebar.tsx` + 纯状态库 `src/lib/fileTree.ts` + `DirLister` 双提供者）
    - 默认关闭的抽屉式目录浏览（桌面 + 安卓平板宽屏）：标题栏 PanelLeft 开关 + 菜单「打开/关闭文件夹」
    - 懒加载（展开才列子项）、目录优先排序、活动标签高亮、脏状态橙点、记住根目录（启动零 I/O）、手动刷新
    - 桌面 = dialog + plugin-fs readDir；安卓 = SAF 桥 openTree + listTree（DocumentsContract），文件经 document URI 走既有打开通道
11. ✅ **Markdown 导出 HTML**（`src/lib/markdownHtml.ts`）：复用 react-markdown + remark-gfm 经 renderToStaticMarkup，
    单文件内联 `<style>`（深/浅一套），零新增依赖；桌面另存 / 安卓 SAF 新建文档 / 浏览器 Blob 下载
12. ✅ **网址导入 Markdown**（导出 HTML 的逆操作）：粘贴 URL → 抓取网页 → 提取正文 → 转成 Markdown 插入新标签页
    - Rust `http_get` 命令（ureq/rustls）：超时 + 5MB 上限 + 仅 http/https + NUL 判二进制；非 UTF-8 页面复用既有编码检测
    - defuddle + turndown(GFM) 动态 import（懒加载 chunk，不进主包）；结果以 `<站名/标题>.md` 分屏新标签页打开，头部写 title/来源 URL/抓取时间
    - 明确不支持：JS 动态渲染的 SPA、需登录的页面；图片保留远程 URL
13. ✅ **界面中英双语**（`src/lib/i18n.ts` + `i18nContext.tsx`）：中文表 `as const` 为 key 源、英文表同构（编译期对齐）；
    偏好并入 settings（`language: system|zh|en`，system 按 navigator.language 解析）；300+ 条文案全量迁移
14. ✅ **字数统计**（`src/lib/wordCount.ts`）：状态栏「X 字 · Y 词」，Markdown 按 CJK 感知计数（Word/WPS 口径），字符按码点计

## v1.0.0 —— 正式发布（发布与工程化）✅ 已完成（2026-09）

15. ✅ **文件关联与单实例**
    - NSIS 经 `bundle.fileAssociations` 注册 42 个文本/代码扩展名的「打开方式」（Editor 角色）
    - `tauri-plugin-single-instance`：二次启动不开启新进程，聚焦已有窗口；
      argv 路径首实例经 `take_launch_paths` 命令取走、二次实例经 `heid-open-paths` 事件转发，前端统一走 openPathIntoTab
16. ✅ **自动更新**
    - 桌面 `tauri-plugin-updater` + `createUpdaterArtifacts`：minisign 密钥对已生成
      （私钥本地保存不入库，公钥内嵌配置，见 `docs/RELEASE.md`）；启动 4s 后静默检查（24h 节流）+「关于」弹窗手动检查，确认后下载安装并经 plugin-process 重启
    - 安卓侧载：复用既有 `http_get` 抓取 `latest.json` 比较版本，有新版提示「前往下载」跳转 Releases 页；浏览器模式无更新通道
    - 发布流程：推送 `v*` 标签触发 release 工作流，tauri-action 产出签名安装包 + latest.json 自动建 Release
17. ✅ **CI**：GitHub Actions（`.github/workflows/ci.yml`）—— vitest + tsc + 前端构建、Windows NSIS 桌面构建（无 secrets 依赖，关闭签名产物）、安卓 arm64 debug APK 构建；`release.yml` 按标签签名发布
18. ✅ **代码结构**：`App.tsx`（原 2743 行 → 现 1695 行）拆分为 9 个 hooks + 纯逻辑库；核心路径补单测

## v1.1.0 —— 可视化内容视图 ✅ 已完成（2026-09）

19. ✅ **CSV 网格编辑器**（`src/lib/csv.ts` + `src/components/CsvGridEditor.tsx`）：`.csv` / `.tsv` 默认进入类 Excel 网格视图
    - 单元格点击输入、框选、行虚拟滚动、行列增删、拖拽填充柄（复制 / 循环 / 等差）、TSV 剪贴板互通、编辑栏、列宽自适应与拖拽
    - 数据事实单一来源仍是 `tab.content`：每一笔编辑走一次 `updateTabContent()`，撤销/重做、脏标记、diff 时间线、草稿全部自动复用
    - 性能闸门：> 5MB 或 > 5 万行默认文本视图
20. ✅ **SVG 可视化工作台**（`SvgWorkbench.tsx`）：左侧源码 + 右侧 blob 隔离预览（脚本不执行），滚轮缩放、拖拽平移、适应面板 / 原始尺寸
21. ✅ **内置图片查看器**（`ImageViewer.tsx`）：智能初始尺寸（大图适应、小图原始）、光标锚点缩放、1:1 切换、底部信息栏
22. ✅ **README 与截图重写**：功能亮点分区 + 6 张界面截图（`docs/screenshots/`）
23. ✅ **Mermaid 图与编辑弹窗**（`src/lib/mermaid.ts` + `MermaidEditModal.tsx`）：预览渲染、编辑弹窗与格式化工具

---

## v1.1.x —— 维护批次 ✅ 已完成（2026-09）

24. ✅ **版本号常量同步**：`src/lib/update.ts` 的 `FALLBACK_APP_VERSION` 更新为 `1.1.0`，与 `package.json` / `tauri.conf.json` / `Cargo.toml` 一致（发布前检查四处版本号）
25. ✅ **未入库内容提交**：Mermaid 编辑弹窗（`feat(markdown)`）、`src-tauri/nsis/` 安装钩子与外壳注册脚本（`build(shell)`）均已入库
26. ✅ **零散修正**：`src-tauri/Cargo.toml` 描述更正为 H.I.D.E 产品定位，去除历史残留文案

## v1.2 —— 大文件与跨文件检索（P0）

主题：**把"打开任意文件"这条路径做到不翻车**。这是用户第一分钟就会遇到、也是同类轻量编辑器被抱怨最多的一环。
设计稿：`docs/superpowers/specs/2026-09-15-large-file-strategy.md`（三层分级：≤32MB 可编辑 / 32~512MB 只读分块预览 / >512MB 拒绝并给替代建议）。

27. ✅ **超大文件打开**（Rust `src-tauri/src/large_file.rs` + `src/lib/largeFile.ts` + `LargeFileViewer.tsx`）
    - probe 单次顺扫统计行数并构建自适应稀疏行偏移表（条目超 2M 步长翻倍稀释，内存 ≤ 16MB）；
      read_line_window 按行窗口读取（CRLF 剥离、超长行 1MB 截断、单窗口 4MB 预算）；UTF-16 按码元扫描换行
    - 前端虚拟滚动（滚动条代理映射 + sticky 行块，超浏览器布局上限按比例压缩）+ 换窗去抖防竞态 + 跳转行
    - 打开路由三层分级统一收敛：选择器 / 最近 / 文件树 / 拖拽 / argv / 会话恢复全部先查尺寸再决定
    - 编辑出口：提取当前窗口到可编辑新标签页；大文件标签不进外部文件监听（避免整读做 diff 基准）
28. ✅ **跨文件搜索**（Rust `src-tauri/src/search.rs` + `src/lib/dirSearch.ts` + `FileTreeSidebar.tsx` 搜索态）
    - `search_in_dir` 按需一次全量扫描（无索引、无常驻后台）：黑名单/隐藏目录与 symlink 跳过，
      >32MB 与二进制文件跳过并计数；逐文件复用既有编码检测解码，按行匹配（大小写/正则/全词，
      正则按行匹配且 regex crate 不支持 lookaround）；结果收集 1000 条上限、计数全量、扫描 2 万文件封顶
    - UI 为文件树侧栏内嵌搜索态（不新增常驻面板）：Enter 触发、分组列表 + 命中高亮 + 统计条，
      Esc/关闭回树；点击命中打开文件并定位行列（`FileTab.jumpRequest` → CodeEditor 挂载后居中选中；
      已打开的脏标签不重读磁盘直接定位）；安卓 SAF 无递归枚举，入口隐藏（regex 仅桌面依赖不进 APK）
29. ✅ **查找上限解除**（`src/lib/searchCore.ts`）：解除 200 万字符扫描与 5000 匹配上限——全量计数、
    「全部替换」走 `replaceAllInText` 全文一次替换不再截断；装饰高亮仍限 5000 条（CM6 渲染约束），UI 显示「共 N 处（仅高亮前 5000 处）」
30. ✅ **小地图大文件适配**（`src/lib/minimap.ts` + `CodeEditor.tsx`）：固定画布高度 + 超阈值停用语法着色 + 只绘制可见行窗口
    - 实测（本机 Edge/Blink 152 headless，与 WebView2 同内核；探针 `scripts/canvas-limit-probe.html` / `.ps1`，可复跑）：
      单个 canvas 单边上限 **65535 设备像素**（65536 起静默失效，与宽度 60~1000、面积至 6555 万像素均无关），
      且 **越界后的 canvas 元素永久失效**——缩回合法尺寸也画不出内容，只有新建元素才恢复；
      折算旧实现（行距 5px + 上下 padding 6px）= **1.31 万行 @dpr1 / 6.55 千行 @dpr2**，与既有文档「约 1.3 万行」一致
    - 旧实现每帧重设 `contentH = 行数 × 5px`，既必然越界（该标签页小地图此后永久空白）又每帧遍历全文行数；
      现在画布尺寸只由「容器可见高度 × dpr」决定（`minimapCanvasDeviceSize`，结构性保证永不接近上限），
      按 `computeMinimapMetrics` 只画可见行窗口（≈ 容器高度 / 行距 行 + 缓冲），绘制量与文档规模无关，
      整篇高度退化为滚动映射用的逻辑值（点击 / 拖拽 / 滑块映射语义不变）
    - 移除了两条「为着色而做」的旧路径：小地图的强制全文解析（原实现最多 7 轮 `ensureSyntaxTree`、单轮超时 5s）
      与「按行数关闭着色」；改为有语法树就按可见窗口着色，未解析到的窗口先画单色行条，
      解析推进时由语法树监听补一次重绘（先单色、后着色）
    - 浏览器验收（真实组件，Vite dev + headless Edge，500 / 2 万 / 20 万 / 300 行四档）：画布恒为 60×800 设备像素、
      20 万行仍有内容绘制、同一 canvas 元素全程未失效
    - 懒高亮（2026-09-15 追加）：大文件（> 200 万字符）不再连带关闭小地图与语法高亮。CM6 的高亮本就是视口级惰性解析，
      成本随「实际浏览过的区域」增长而不是文件大小——实测 12.7MB / 31.5 万行 / 1330 万字符的
      `node_modules/mermaid/dist/mermaid.js`：打开瞬间语法树只覆盖 **0.775%**（约 10 万字符）而视口已着色；
      整篇强制解析一次（等价于从头滚到尾的极端情况）滞留 ≈ **2.3 字节/字符**（≈31MB，瞬时峰值 ≈128MB），
      且解析完整篇仍只渲染约 500 个高亮片段；验收：`lowPerf` 下该文件编辑器视口着色 601 个片段、
      小地图出现 token 色（242 种颜色）。大文件降级现在只剩：补全/选区匹配/自动缩进（>1000 行即关）
      与粘性滚动（每帧回扫光标行，成本随行数增长）；状态栏「大文件」提示与设置面板小地图说明同步更新
    - 手工验收样本（仓库内现成、无需生成大文件）：`node_modules/katex/dist/katex.js`（17,741 行 / 0.59MB，跨过旧 dpr1
      失效点且未过 200 万字符线，是最灵敏的回归样本）、`node_modules/d3/dist/d3.js`（20,626 行 / 0.57MB）、
      `node_modules/dompurify/dist/purify.cov.cjs.js`（26,428 行 / 0.55MB）、`package-lock.json`（7,786 行，跨着色阈值）、
      `node_modules/mermaid/dist/mermaid.js`（315,016 行 / 12.7MB，验大文件下小地图保留）
    - 追加修正：小地图与查找栏原先各自用 `StateEffect.appendConfig` 往编辑器上追加 updateListener，而 **CM6 的
      `StateEffect.reconfigure` 会丢弃 appendConfig 追加的扩展**（本组件的 extensions 依赖 value，每次编辑/语言加载/
      设置变更都会重建根配置）——监听器被静默丢掉后表现为「滚到新区域文本已高亮、小地图却一直是单色，
      再滚一下才恢复」（DOM scroll 监听器不受影响）与「查找栏编辑文档后计数不更新」。改为 CodeEditor 统一的
      视图更新订阅中心（挂在根配置里、随 reconfigure 重建）：小地图按「文档/尺寸变化 → 重绘、纯选区 → 只更新滑块、
      语法树推进 → 补一次着色重绘」分流，查找栏恢复跟随文档与选区。验收：查找栏编辑后计数 1/3 → 1/4；
      小地图跳转后自动从 21 色变 594 色（无需再滚动）
    - 遗留（留待 v2.0 工程化收口）：`extensions` memo 依赖 `value`，每次编辑都会重建根配置，既有性能代价，
      也是上面「追加扩展被丢弃」的土壤；应改为 Compartment 或按需重建
31. ✅ **十六进制查看**（`HexViewer.tsx`）：只读 hex 视图（16 字节/行，偏移 + hex + ASCII），二进制判定升级为
    16KB 采样（大文件里 NUL 可能藏在深处），与「二进制 · 只读」状态衔接
32. ✅ **撤销历史自适应降档**（`src/lib/tabHistory.ts`）：内容超 400 万字符自动把 200 条全文快照降至 5 条，
    状态栏提示；50MB 级文件连续编辑不再线性吃内存

## v1.3 —— 结构化内容与新格式（P1）✅ 已完成（2026-09）

主题：**把"打开即能看懂"扩展到更多文件类型**，这是与纯文本编辑器拉开差距的地方。
设计稿：`docs/superpowers/specs/2026-09-16-v13-structured-content-design.md`。
新增依赖仅 js-yaml（懒加载 chunk 60.8KB / gzip 18.3KB，不进主包）；JSON 用原生 JSON.parse。

33. ✅ **JSON / YAML 结构化视图**（`src/lib/jsonTree.ts` + `JsonTreeViewer.tsx`）
    - 双视图模式对标 CSV（`jsonView: 'tree' | 'text'` 挂 FileTab）：可解析且 ≤200 万字符默认树视图，
      解析失败/超限默认文本；工具栏"树/文本"切换 + "格式化"按钮（缩进规范化走 updateTabContent，可撤销）
    - 树浏览：默认展开前两层、折叠态显示 `{n}`/`[n]` 计数与值摘要、全部展开/收起、
      双击标量复制原值；单容器直接子节点 >500 只渲染前 500（折叠态零渲染成本）
    - YAML 经 js-yaml 动态 import 解析/序列化（dump 固定 2 空格，YAML 禁 Tab）；
      非字符串键 String 化、循环引用（anchor &alias）以 seen 集合截断为占位节点
34. ✅ **Markdown 增强**
    - **大纲侧栏**（`markdownOutline.ts` + `MarkdownOutline.tsx`）：ATX 标题扫描跳过围栏代码块
      （CommonMark 同长度规则），按层级缩进列表；点击分流——预览可见时按 `[data-md-start]` 滚动定位
      （`MarkdownPreviewHandle.scrollToOffset`），编辑器可见时复用 `jumpRequest` 跳行（分屏两者都做）
    - **粘贴图片自动落盘**（`markdownImagePaste.ts`）：桌面 markdown 标签页粘贴 image/* →
      写 `<文档目录>/assets/paste-时间戳(-n).<ext>`（fs_mkdir 幂等 + createNew 防覆盖）→ 光标处插入
      `![](相对路径)`（含空白/括号自动尖括号包裹）；未保存文件提示先保存；浏览器模式转 data URI（≤2MB）；
      安卓 SAF 无二进制写桥不启用。预览新增 `baseDir`，相对图片地址拼文档目录读取（`joinRelativeSrc`），
      与既有 `|||LOCAL-FILE:` 绝对路径机制共存
    - **导出 HTML 带代码高亮**：markdownHtml 管线加 code 组件，react-syntax-highlighter（Prism）
      输出内联着色 span，主题随导出深浅（oneDark/ghcolors）；行内与无语言代码块维持普通样式，
      以"children 尾部换行"区分围栏块与行内代码
    - **图片本地化**（`imageLocalize.ts` + Rust `http_get_binary`）：收集 `![](http…)` 唯一图源，
      逐张下载（base64 回传，复用 http_get 护栏：仅 http/https、15s 超时、5MB 上限、文本响应拒绝）
      → 存 `assets/remote-<url hash 8 位>.<ext>` → 全文替换相对路径；单文档上限 50 张，失败逐张跳过计数，
      结果通知；入口在菜单"图片本地化"（仅桌面 + 标签页有路径）
35. ✅ **CSV 只读态排序 / 筛选**（`csv.ts` 纯函数 + 网格集成）
    - **视图变换不改写数据**：`computeRowOrder` 产出"显示行 → 原始行"映射；
      排序数值/文本感知（数值按数值比，否则 localeCompare numeric）、空值恒排最后（升降序一致，Excel 口径）、
      等值稳定；筛选为不区分大小写子串匹配任一列；表头行恒在最前
    - 排序入口在列标右键菜单（升序/降序/取消，列标 ▲▼ 指示），筛选输入嵌在编辑栏（漏斗图标，Esc 清空）；
      行号显示**原始行号**，状态栏显示"已显示 N / M 行"
    - **结构操作降级**：排序/筛选生效期间增删行列、剪切、清空、粘贴块、填充手柄禁用（菜单置灰），
      单元格内容编辑仍可用（经映射写回原始行）
36. ✅ **打印 / 导出 PDF**（`printDoc.ts`）：隐藏 iframe + `contentWindow.print()` 调起系统打印
    （WebView2/Chromium 打印对话框自带"另存为 PDF"），afterprint 后清理 iframe
    - 内容路由：markdown 复用 renderMarkdownToHtml（含代码高亮）；csv 经解析层转全量 HTML 表格
      （打印文档而非屏幕转储，不随筛选视图）；其余文本/代码行号等宽 pre（pre-wrap 分页友好）
    - 入口：菜单"打印…" + `Ctrl+P`；二进制/大文件预览禁用，安卓无打印对话框不显示

> v1.3 后续细化（同月）：JSON/YAML 增加分屏视图（`jsonView: 'split'`，左结构树右源码三档切换，
> 两侧按比例同步滚动）；Markdown 大纲改为贴预览视图右缘的毛玻璃小弹窗，收展合并到常驻把手
> （收起为右移 10px 避让预览滚动条的 "<"、展开后贴面板左缘变 ">"，顶部工具栏大纲开关移除；
> 面板与把手圆角均朝左——贴右缘故右侧直角，面板滚动条用全局 heid-scroll 细样式，
> 容器 pointer-events 穿透不挡预览滚动条，滚动跟随高亮当前标题）；修复粘贴图片预览失败（Windows 混合分隔符路径 fs 插件读取失败 →
> 路径归一跟随文档目录原生分隔符）与页签文档高亮错位（块级图片 URL rewrite 改变了 data-md 偏移基准 +
> 跨块选区整块误包 → 移除 rewrite 收敛到 img 组件解析、行内操作按"块内选中文本"逐块应用）；
> 右键菜单新增「粘贴图片」（markdown 专属：剪贴板读图 → assets 落盘 → 插入相对路径）；
> 加载失败的图片同样可右键删除/剪切；从文档删除受管理图片（paste-/remote- 命名）8s 后自动清理本地文件。
> 分屏同步滚动掉帧修复：react-markdown v10 的 Markdown 组件内部无缓存，App 级任意 setState
> （打字光标 / 大纲高亮）经内联闭包 props 击穿预览 memo 后全篇重跑 remark/rehype——
> 按块 MdBlock 记忆化（md 源串与 components 不变即跳过重解析）；大纲滚动跟随从 App 下沉到
> MarkdownOutlineLive（仅展开时挂载并监听预览滚动，高亮变化只重渲染大纲自身）；
> 大纲条目支持按标题层级折叠（有子标题才显示折钮，面板收展不丢折叠状态），面板内容与上下边缘留出间隙；
> 大纲右键菜单可一键展开/折叠全部小节（无可折叠项或已处于目标状态时对应项置灰）。

> **v1.3 路线图外批次（同发布窗口完成）**：
>
> **标签拖拽与多窗口**（设计稿：`docs/superpowers/specs/2026-09-17-tab-drag-windows-design.md`）——
> 浏览器式标签体验（仅桌面）：栏内重排（Chrome 式让位 + 平滑滑动）、拖出脱离成窗
> （落点 = 光标，唯一标签源窗自毁）、跨窗口合并（占位符指示 + ack 回执协议）、
> 右键「移到新窗口」、按窗会话恢复（快照按窗口隔离 + manifest，含窗口位置尺寸）。
> 拖拽采用原生 HTML5 DnD（OLE，系统拖拽图像全局跟手），载荷经 Rust 暂存区传递；
> 顺带修复关闭激活标签闪现欢迎页。遗留问题与回归基线记录于
> `docs/bugs/2026-09-17-native-tab-drag-issues.md`。
>
> **SVG 可视化编辑**（设计稿：`docs/superpowers/specs/2026-09-18-svg-visual-editing-design.md`）——
> 工作台升级「编辑 | 预览」双模式（`svgEdit` 随页签持久化）：画布点选 / 拖拽移动（松手才提交）/
> 方向键微调 / 删除，检查面板三页签（属性：fill/stroke/粗细/透明度/文本；图层：只读结构树联动；
> 颜色：调色板 + 一键全局换色），PNG 导出（1x/2x/4x 存盘或复制）。写回为「手术式」：
> 区间扫描器记录元素源码区间，修改只替换该元素文本，注释/缩进逐字保留，
> 撤销/保存/草稿零新增接线；渲染副本 sanitize（脚本不执行、无外链），解析失败退回纯预览。
> 画布平移改中键拖动，编辑|预览分隔条可拖调宽（[20%, 80%] 持久化）。
>
> **文件树与窗口体验**——文件树递归监视自动刷新（DirLister.watch，刷新保留子目录展开/错误态，
> 同批合并 + seq 防并发回退）；跨文件搜索增强（扫描移出主线程、CJK 感知命中窗口、
> 结果分页、侧栏可调宽持久化）；会话恢复并行读盘 +「恢复中」占位（不再先闪欢迎页）。
>
> **更新体验改造**——更新检查去 24h 节流改启动必检（节流曾致发版后 24h 内启动收不到提示）；
> 通用通知卡片承载更新 / 本地化进度；更新说明改为瞬态只读文档标签
> （更新重启后自动打开一次，不进会话快照），「关于 → 更新文档」可重看与回看历史版本（≤20 份）。

> 同批次候选（有余力再做）：剪贴板监控（Notepad3 PasteBoard 式的「最小化粘贴板」），
> 做成设置项默认关闭，避免打扰；「复制为富文本（保留高亮）」与之同源，可一并考虑。

> **v1.3.1 补丁**（随修随发）：发行说明写入机制随 v1.3.0 上线，v1.2 直升客户端无落盘——
> 更新重启后不自动展示文档、「关于 → 更新文档」入口不渲染。启动自愈：当前版本在
> 文档列表缺失且 latest.json 版本恰为当前版本时补种（`maybeSeedCurrentVersionNotes`）。
>
> **v1.3.2 补丁**（随修随发）：大目录场景可用性——跨文件搜索三重提速（头部采样
> 二进制快筛 / 免逐行分配匹配 / rayon 并行两遍扫描，数万文件目录 30 分钟级→3 秒级）
> + 进度轮询与取消 + 结果上限 20000；文件树行虚拟化与目录监听 watchImmediate 化
> （监听建立曾同步扫全树致整窗冻结 8.5s）；启动窗口按需显示消除白屏闪帧；
> 只读预览右键不再弹浏览器菜单。发行说明自本版起按完整版本号单独成文
> （docs/RELEASE-NOTES-v1.3.2.md），工作流不再按次版本号复用旧文档。

> **v1.3.3 补丁**（随修随发）：CSV 网格编辑器批次——修复同根因的编辑焦点三连
> （双击编辑开启当帧即关、编辑栏打一字即被误提交、退格清空整格）与向上拖行
> 按目标行数截断网格的丢行事故；随后补齐交互能力：列标 / 行号拖拽重排（落在
> 列 / 行上互换，对准网格线插入式移动）、Ctrl 多选（复制 / 清空 / 铺满作用于
> 全部选区）、「自适应表格大小」（列宽按内容实测 + 行高按折行数 + 自动开启
> 换行，附独立换行开关）、逐行行高拖拽（行号底边，双击恢复，持久化）、
> 单元格 Alt+Enter 换行（编辑框升级多行文本域，顶部编辑栏同步多行）。
> 快捷键弹窗新增「CSV 表格」分组。发行说明：docs/RELEASE-NOTES-v1.3.3.md。

> **v1.3.4 补丁**（随修随发）：CSV 网格编辑器两修——键入进编辑字符次序颠倒
> （单击选中后打字「20」得「02」，程序化聚焦光标在文本开头所致，现落末尾，
> F2 / 双击进编辑同落末尾合 Excel F2 语义）；多行纯文本粘贴被按 TSV 拆行
> 覆盖下方行——单格目标整段含换行进一格（无制表符多行启发式，先框选区块再
> 粘贴仍是拆行出口），剪贴板升级双 MIME（text/html 表格结构随复制 / 剪切写出、
> 粘贴优先解析，多行单元格在网格 / Excel / Sheets 间往返不散架），编辑态右键
> 粘贴插入编辑框光标处不再先提交。发行说明：docs/RELEASE-NOTES-v1.3.4.md。

## v1.4 —— 移动端补齐 ✅ 已完成（2026-09）

主题：**安卓从"能看"到"能用"**。桌面端本版零改动（触屏分支全由 `IS_TOUCH_PRIMARY` /
`isPhone` / `pointer-coarse` 门控）。五批次交付：

- **触控基建**：`useLongPress`（500ms、位移/捏合/滚动让位、吞掉抬手合成的 mousedown）+
  `ContextMenu` 触屏变体（行高 44 / 面板 280）；长按接线到标签栏 / 文件树 / Markdown 预览 /
  大纲 / JSON 树 / CSV；编辑器与预览长按文字让位系统选字；hover 样式收进
  `@media (hover:hover) and (pointer:fine)`，页面自定义滚动条只留桌面；
  快捷键提示按物理键盘门控（Kotlin `InputManager` 枚举 + `heid-hwkb` 事件）。
- **手机端补齐（37）**：文件树覆盖式抽屉 + 顶栏入口（图片查看入口随之回归）、精简状态栏、
  键盘避让（IME 高度经 `--heid-kb` 注入，底栏沉后 / 信息栏贴键盘上沿 / 光标滚入可视区）。
- **SAF 文件管理（38）**：`treeManageAvailable` 放开双端，Kotlin 桥 `createInTree` /
  `renameEntry` / `deleteEntry`；`fsRename` 返回生效新路径（改名后旧 content URI 失效）；
  树根行解码 docId 显示真实目录名；系统不提供的能力（剪切 / 复制 / 打开所在目录）置灰。
- **查看器与 CSV**：`usePinchZoom`（单指平移 + 双指捏合，锚点=双指中点）接 ImageViewer、
  SvgCanvas、SvgWorkbench 预览面板；CSV 网格长按菜单 + 选区延展把手替代填充柄，
  全部拖拽改 Pointer 事件系（WebView 拖拽不发 `mousemove`）。
- **平板与弹层**：文件树改桌面同款推拉式、菜单栏常驻纯图标按钮 + 系统分享（`ACTION_SEND`）、
  设置 / 关于弹窗触屏档、Markdown 选区工具条（复制 / 粘贴 / 全选 / 重复上次格式化 / 更多）。
- **release 签名（39）**：独立 keystore 不入库、`build.gradle.kts` 自动接入、CI `android-release`
  任务恢复；与 v1.3.x 的 debug 包签名不同，跨该边界需卸载重装一次（已写进 README 与发行说明）。

## v1.4.1 —— v1.4 触屏适配收尾（补丁，随修随发）

> 原 v1.5 的「平台决策 + 移动端收尾」经 2026-09-21 复核后降级为补丁：这两项**没有主题**，
> 是 v1.4 触屏适配没收干净的尾巴，按「补丁版随修随发」处理。
> 但补丁同样要走完整发布流程（六处版本号 + CI + 签名 + 单独成文的发行说明），
> 因此**攒成一批一次发**，不为零星小修单发一次。

40. ✅ **平台决策：不补 macOS / Linux，明确「Windows + 安卓单平台做深」**
    评估依据（2026-09-21，均可复查）：
    - `tauri.conf.json:43` 的 `bundle.targets` 只有 `nsis`；`release.yml` 的 `ubuntu-latest` 仅用于安卓
      交叉编译，桌面产物只有 `windows-latest` 一条线
    - 2026-09-18 起弃用 tauri-action、`latest.json` 改为手写签出（`release.yml:41,66`），
      多平台意味着要手写多份平台条目
    - macOS 绕不开付费公证（Apple Developer 99 美元/年），否则 Gatekeeper 直接拦；证书还要入 CI secrets
      —— 本项目已有一处「唯一副本密钥」隐患（安卓 keystore 只有本地一份），不宜再加一套
    - Linux 的根本障碍是 **WebKitGTK 不是 Chromium**：至今全部前端验证都在 Blink 内核上做
      （用 headless Edge 当 WebView2 的同内核代理，见 v1.2 条目 30 的实测记述），
      而 Markdown 预览查找依赖 CSS Custom Highlight API，WebKitGTK 支持很差；
      等于要在一个从未测过的渲染引擎上重验整个界面，且手上没有 Linux 用户可验证
    - 待办：把平台支持声明写进 README（本批次内完成）
41. ✅ **v1.4 触屏遗留（按代码实测重写 —— 原记述有误）**
    - **Markdown 表格行列增删**：触屏出口**自 v1.1.0 起就已存在**（`MarkdownPreview` 的触屏表格
      结构工具条，`IS_ANDROID_APP` 门控，单击单元格即浮出，行+/行−/列+/列−）。真实问题不是
      「没做」而是**做了从未量过**：按钮 32px 高、字号 11px，低于 v1.4 自己定的 ≥44dp。
      → **已收口**：整条定宽 240×46dp、四个动作各 58–62×44dp；**落点从「算格子坐标」改为
      fixed + `calc(var(--heid-kb) + 12px)`**——量的时候发现按「点击那一刻可视框」算出的浮层
      会在键盘弹起（IME insets 异步压矮预览区）那一刻被压到键盘底下，等于按钮消失；
      无可删项时（表头唯一行 / 唯一列）不再产生空改动、不白标脏。补 10 例测试。
    - **SVG 元素微调**：✅ **已收口**——`SvgInspector` 属性页新增「位移 X/Y」两行
      （读写元素**前导 translate**，归零时连属性一起删，源码里不留 `translate(0 0)`），
      两侧配 ± 步进按钮，步长 1 SVG 用户单位（与 `SvgCanvas` 方向键默认档同值）；
      步进走与画布拖拽同一条 `applyTranslate` 合成路径，撤销/保存/草稿零新增接线。
    - **平板标签关闭钮 28×28**：**不是遗留，是当时的有意妥协**（标签内扩到 48dp 会压到邻标签，
      长按标签有菜单作为替代）→ 已移入下方「明确不做」，零代码
    - 文档修正：本条目原记述、`docs/RELEASE-NOTES-v1.4.0.md`「已知限制」中的对应条、
      触屏设计稿的「交付状态」段（同样写着「无触屏出口」）。
      **已发布的 GitHub Release 正文不动**——已知限制多列一条是安全的错，少列才是问题
    - 平板实测（1280×800 · 字体 1.3× · 真 touch 事件 + CDP 量取）：两处新界面在整屏
      touch-target 审计里 **0 项 <44dp**；`--heid-kb` 报 368dp 时浮层底边在 420dp（键盘之上）、
      实点 Row+ 生效（2 行 → 3 行）；检查面板 ± 各 44×44、X/Y 输入框 68×44、面板 244dp 宽
      无横向溢出；改 X=5 后源码为 `transform="translate(5 2)"` 且同行 `x="10" y="10"` 与注释逐字保留
    - **本批次新发现、未做**（记入 v2.0 工程化批次或后续补丁，别再当「已收尾」）：
      源码编辑器行内的**取色圆点 11.9×11.9dp**（`cm-colorDot`，点开取色器的唯一入口），
      平板上极难命中；桌面/触屏同一控件，改大要连带重验色点与文字的排布

### 从 v1.4.1 移出、暂挂候选（不进任何一版，待 v1.5 落地后重估）

- **源码编辑器行号槽触屏适配**（`CodeEditor.tsx:1498` 的 `if (!IS_ANDROID_APP)`）：
  原以为是收尾，实测不是。`setupLineNumberInteractions` 位于 `CodeEditor.tsx:543-695`（153 行），
  **全部挂在 mouse 事件系上**（`mousedown` / `mousemove` / `mouseover` / `mouseout` / `mouseleave` / `mouseup`，
  一个 pointer 事件都没有）。而 v1.4 的 CSV 批次已实证「WebView 拖拽不发 `mousemove`」——
  仅解除门控则拖动多选根本不工作，必须先把这 153 行迁到 pointer 事件系，
  **而这会动到桌面路径**，与历代版本一直守的「桌面零回归」约束冲突，需配桌面回归测试。
  另外 44dp 触点要求加宽行号槽（挤压正文宽度），而命中判定靠 `elementFromPoint` 打
  `.cm-gutterElement`（`:556`），宽度一变需重验。
  方向已定为**只做平板（≥768px），手机维持关闭**（手机上 44dp 行号槽吃掉太多正文宽度，
  且触屏有系统选字与长按菜单兜底）；但排期押后：设备互联落地后，平板的主要用途会从
  「看本地笔记」变成「浏览与修改电脑上的代码文件」，那时才好判断这项的真实价值。

## v1.4.2 —— 补丁批次（✅ 2026-09-23 发布；48 / 49 / 50 / 52① / 53 已完成并实测，51 与 52② 归入 v1.5）

> 编号接全局计数（42–47 已被 v1.5 占用），不按文档顺序排。
> 这一版是「随修随发」的补丁版，条目按优先级排列。

48. ✅ **安卓：外部应用「打开方式」选中后文件不加载**（头号；详见
    `docs/bugs/2026-09-22-android-view-intent-not-handled.md`，修复记录在该文末）
    - 根因不止一条：**①** 入向 intent 从来没被消费（manifest 的 4 组 filter 只负责让应用出现在列表里，
      `MainActivity` 唯一的 `ACTION_VIEW` 是**往外**开链接用的）；
      **② 2026-09-23 装机才暴露**：Android 匹配只看提供方报出的 MIME，自动生成的 5 组只声明了
      `text/plain`/`text/html`/`application/xml`/`application/json`/`image/svg+xml`，
      而 `.md` 报 `text/markdown`、`.py` 报 `text/x-python`、`.csv` 报 `text/csv` → **压根不出现**
      （实测 `query-activities` 0 条命中，Files 里点 `.md` 原地不动）
    - 修法：Kotlin 侧 `onCreate` 读 intent + `addOnNewIntentListener`（**不能覆写 `onNewIntent`**，
      生成的 `TauriActivity.onNewIntent` 是 final）；条目压进队列，前端 `HeidBridge.takeLaunchFiles()`
      一次取空 + `heid-view` 事件催第二次——队列是唯一事实源，冷启动丢事件与 StrictMode 双跑都不会重开。
      **没有复用 `heid-saf`**：那条通道上有一次性监听器，凭空的事件会取消用户正开着的文件选择器。
      MIME 用自动生成标记**之外**的手写 `text/*` filter 兜住（放标记外才不被构建重写）
    - 已定的两条口径：**重复打开**——目标标签脏则只切前台不覆盖，干净才重读磁盘
      （只作用于外部交来的路径）；**文件树根目录不跟随**——单文档授权不等于目录授权
    - 顺带接了 **分享**（SEND / SEND_MULTIPLE，manifest 本来就声明了）：附件走 `EXTRA_STREAM`，
      纯文本走 `EXTRA_TEXT` → 落成新的未命名草稿
    - 实测（x86_64 debug 包 / Android 15 模拟器 / 1.4.2·1004002）：冷启动 `am start`、冷启动真点 Files、
      热启动第二个文件、脏标签不被盖、干净标签重读磁盘、真分享面板分享文件、分享纯文本成草稿、
      `text/markdown` 由 0 命中变 1 命中、手写 filter 构建后仍在——全部 ✅
    - 口径收口：v1.4.1 已发布正文里那句「修复了…文件关联」只成立一半（`106edc3` 轮换 MIME 影响的是
      **列不列得出来**）。已发布内容不动，本版说明里写清「修复了从外部应用打开时文件不加载」

49. ✅ **桌面与安卓互相引流：两端各放一个「获取另一版」面板**（v1.5 设备互联的前置位）
    - 落点按建议放在**两端「关于」弹窗的同一块区域**（更新检查与更新文档之下、版权行之上），
      不新开一级入口、不动菜单结构；组件 `src/components/OtherPlatformPanel.tsx`
    - 桌面 → 移动端：**APK 二维码** + 门槛说明（Android 7.0+ / 仅 arm64）+ 复制链接；
      移动端 → 桌面：**只有「打开下载页」**——地址本身已整行可见可长按选中，
      而「关于」里更新检查失败时本来就有一个「复制下载链接」，再放一个是重复
    - 二维码指向 `apkMirrorDownloadUrl(version)`（Gitee 镜像按 `v<版本>` tag 的直链，
      与 `MIRROR_RELEASES_PAGE` 同源拼接），**不跟 GitHub**：扫码的手机多半在国内网络里。
      代价是**每个版本都必须走完 `mirror-gitee` 任务**，否则扫码 404——已并进第 54 条的发版核对
    - 版本选择：`updater.latestVersion ?? appVersion`（检查到更新就编最新那个，否则编当前）
    - 懒加载已落实：`QrImage` 走 `React.lazy`，独立 chunk **9.9 KB gzip**；
      主入口 `index-*.js` **791.5 KB gzip** 未受影响；安卓上实测该 chunk **根本没被请求**
      （`performance.getEntriesByType('resource')` 里没有 QrImage）
    - 实测：桌面（`tauri dev` + WebView2 CDP）「关于」里二维码渲染正常，
      **按元素裁剪屏幕像素后用真实解码器 jsQR 读回**，结果逐字符等于那条 Gitee 直链；
      140 CSS px 的显示尺寸下解码通过。安卓（模拟器 1.4.2 包）面板显示
      「桌面版（Windows x64）」+ 地址 + 打开下载页，无重复按钮
    - 顺带补的一处：「关于」卡片加了 `max-h-[calc(100vh-1.5rem)] overflow-y-auto`——
      面板让卡片高了约 200px，手机上原本就只剩十几像素余量，矮屏与横屏会裁掉版权行
    - 与 v1.5 的衔接：设备互联的「扫码连接」将来就长在同一块区域，用户心智位置不用再挪

50. ✅ **安卓端跨文件搜索入口未门控**：文件树搜索按钮跟 `canManage` 走，而
    `App.tsx` 传的是 `isTauri`（安卓也是 true），于是入口照常出现、SAF 路径下什么都搜不到。
    已在 `FileTreeSidebar` 内按 `canManage && !IS_ANDROID_APP` 门控（与同文件里 `noSafMove` 同一写法）。
    验证：新增 `FileTreeSidebar.android.test.tsx`（安卓不放搜索入口、刷新仍在）2 例 +
    桌面侧原有 `FileTreeSidebar.test.tsx` 8 例仍绿；模拟器手机档开真实 SAF 目录实测顶栏只剩刷新 + 关目录
    - 十六进制查看同理依赖真实路径，但它只在打开二进制大文件时经 `LargeFileViewer` 出现，本版未动

51. **语言高亮覆盖：补包还是就写 18 种** → **延期到 v1.5**（2026-09-23 定，不进补丁版）：
    `LANGUAGE_LOADERS` 只有 18 个键，
    Go / Shell / C# / Ruby / Swift / Kotlin / Scala / SCSS / Less / TOML / INI / Dockerfile / Makefile
    这 13 种能在状态栏认出类型但没有着色器，按纯文本渲染。README 已按实数改写。
    可补的：Go / C# 有官方 `@codemirror/lang-*`，Shell / TOML / INI / Dockerfile / Makefile 走
    `@codemirror/legacy-modes`；Ruby / Swift / Kotlin / Scala 只有社区包。
    补完才谈得上把「30+」写回宣传语——语言包都是懒加载 chunk，**不减安装包体积**（此句 2026-09-23 勘正，
    理由与门禁写法见 v1.5 小节条目 51）
    - 延期的理由：这是功能批次（要动着色器注册表、要逐种语言实测），塞进「随修随发」的补丁版
      会把发版拖长；且 README 已按 18 种写，**没有虚假宣传要救**

52. **触屏尺寸遗留**（v1.4.1 实测发现）——**① 已于 v1.4.2 解决，② 推到 v1.5**
    - **① 取色器在触屏上不可达**：源码行内的取色圆点是 `0.85em`（实测 **11.9×11.9dp**），
      而它是打开取色器的**唯一入口**。圆点撑不到 48dp——一行行高只有 19.6dp，
      横向再扩就会吃掉邻字、害光标落位。所以按「必须提供替代」办：
      **长按选中色值 → 选区工具条上多一个「取色」按钮**（实测 76×48dp）。
      命中判定在 `colorDotExtension.ts:colorLiteralCovering()`：按**重叠**找、按**整条**回，
      因为长按只会选中 `ff6a00` 这种不含 `#` 的词，而取色器要改写的是完整字面量。
      入口同样受「颜色标记」开关控制（与圆点一致）。实测见下方条目末。
    - **做 ① 时查出的更大一条**：选区工具条以前挂在 `markdownMenu` 上，
      而系统选区弹窗是在**所有**触屏编辑器里统一屏蔽的（`CodeEditor.handleEditorContextMenu`
      里 `if (IS_TOUCH_PRIMARY) preventDefault()`）——两边一叠加，
      **代码文件里选中文字后既没有系统弹窗、也没有这条工具条，复制 / 粘贴 / 全选全都没有**。
      现已改为所有编辑器都出现，并按他的要求**与 markdown 同一套操作逻辑**：
      「更多」对 markdown 开格式菜单，对其余编辑器开**桌面右键那份通用编辑菜单**
      （撤销/重做、剪切/复制/粘贴、全选、查找替换、切换行注释、转大小写、清除行尾空格、
      向下复制行、上移/下移一行、删除行、选中所有相同匹配、折叠/展开全部），
      入口共用 `buildEditorMenuItems()`，不另建一份清单
    - **实测**（1.4.2 debug 包，手机档 412 CSS px + 平板档 1280 CSS px 两台壳都量了）：
      CSS 文件长按色值 → 浮出 复制·粘贴·全选·**取色**·**更多**（带文字的按钮 76×48dp，
      「更多」48×48dp）；点「更多」→ 通用编辑菜单 18 项、**条目最小高 48dp**、
      菜单打开后选区仍保留（状态栏「行 2, 列 17（选中 6）」）；点「转为大写」→
      `#ff6a00` 变 `#FF6A00`，底栏撤销可回退；点「取色」→ 取色器打开且色值框是**完整**的
      `#ff6a00`。markdown 文件仍是 复制·粘贴·全选·更多（+最近使用），无回归
      - 两台模拟器都显示 `Ctrl+…` 提示，是因为 AVD 的键盘被 `hasHardwareKeyboard()` 判为
        物理全键盘；真机（Tab S8 / Mate 40 Pro 无外接键盘）走 `heid-hwkb` 不显示，不是回归
    - **② 弹窗层提档**（推到 v1.5）：设置 / 关于 / Diff / 图片插入 / 确认框、CSV 网格菜单、
      Markdown 表格结构条仍在 44dp 档，未统一到手机/平板主触点的 48dp。
      提档要重跑那批弹窗的测试与字号，且 v1.5 做设备互联时平板 UI 本来就要重看，一起量更省。
      「获取另一版」面板的按钮是按「关于」现有的 44dp 档写的，②做的时候一并抬
    - 行号槽触屏版**不在本版**（见上方「暂挂候选」）

53. ✅ **手机顶栏那颗点用错了颜色**（v1.4.2 修，2026-09-23）：此前记为「脏标记误报·待查」，
    他复核截图时才看清真正的问题在**颜色**上——标签上那颗点是四态的
    （`TabBar.tsx:310-317`：外部改动冲突=橙+光环 / 未保存=琥珀 / **当前且干净=绿** / 其它标签=灰），
    抽屉里的列表（`mobile/TabSheet.tsx:48-57`）也是同一套；
    唯独**手机顶栏**（`mobile/TopAppBar.tsx`）只做了两态「脏=琥珀 / 干净=灰」，
    而那一行显示的永远就是当前标签 → 当前文件被标成灰点，看着像「没打开/不对劲」，
    跟抽屉里同一个文件的绿点对不上。桌面与平板走 `TabBar` 所以正常，**只有手机错**。
    修法：干净态改 `bg-emerald-500`，与另两处同源。
    - 为什么当初误判成「脏标记写错」：他报的是「刚打开、没动过的标签显示未保存」，
      而只读的「更新文档」标签也这样 → 像是初始化路径的问题。实际是绿点该亮却亮了灰点，
      被读成了「未保存」。**这条本身是个教训：颜色语义不一致会被读成功能错误**

54. **国内镜像的首次实战**：本版是第一个走 `mirror-gitee` 任务发版的版本。
    脚本与 CI 已就位并用 v1.4.1 产物手工跑通两遍（含幂等重建分支），缺的只是 CI 环境里的那一次。
    发版时核：Gitee 上 `v<版本>` 的两个附件、`mirror-latest/latest.json` 的 `version` 与
    `platforms.*.url` 指向 Gitee、以及桌面端应用内更新能从镜像清单里拿到合法签名

55. **桌面端「覆盖脏标签」的口子**（48 号顺带查出，本版只在安卓外部打开路径上避开）：
    `openPathIntoTab` 命中已打开的标签时直接用磁盘内容替换并清掉脏标记，
    而这条路径桌面有三处入口（文件树点击、最近打开菜单、二次实例 argv）。
    也就是说：在桌面改到一半没保存，再从文件树点同一个文件，改动就没了。
    修法与安卓侧一致（脏则只聚焦），但要动桌面行为、得配桌面回归测试

56. **「打开方式」还差两类提供方**：报 `application/octet-stream` 的管理器（`.svelte`/`.kts` 等冷门扩展）
    与直接给 `file://` 的旧管理器（无 MIME，任何带 mimeType 的 filter 都不匹配）。
    覆盖要声明 `*/*` 或纯 scheme filter，代价是在所有文件类型（含图片、压缩包）里出现，本版刻意不做

57. **外部打开的标签在应用被杀后可能恢复不了**：提供器只给临时授权时 `takePersistableUriPermission`
    会抛异常（已吞掉），本次进程内能读能存，杀进程后该 URI 失效、会话恢复会静默丢掉这个标签。
    待办不是「修好」而是**别静默**：恢复失败时给一条「上次从外部打开的文件授权已过期」的提示

58. **平板档文件树顶栏未重量**：50 号删掉了搜索入口，顶栏从三个按钮变两个；
    手机档已实测（两行排布不变形），≥768px 那一档是纯删项、风险低但没量过

## v1.5 —— 设备互联（局域网直连）

主题：**手机接着编辑电脑上的文件**。扫码连接，无账号、无云中继、不出局域网。
设计稿：`docs/superpowers/specs/2026-09-21-device-link-design.md`（方案层面的决策已全部收敛，见其第 2 节与第 12 节）。

三条已定的地基：**桌面恒为服务端、手机恒为客户端**（手机当服务端会被 Doze / App Standby 杀后台监听，
可靠性不可控；「存回桌面」由手机发起写请求实现，不需要手机监听端口）；**开关按需启停、不引入常驻后台**
（原写作「手动开关」，2026-09-23 按下方第 1 条改为「记忆并启动自动 bind」）；**二维码携带地址，
因此不做自动发现**（顺带避开 Android 组播锁 `MulticastLock` 与各厂商 ROM 差异、以及公司网 / 路由器 AP 隔离吞组播的问题）。

### 2026-09-23 定稿：范围、发布拆分、以及原设计稿留下的五条待确认

**范围**：42 / 43 / 44 是本版主题，另挂 51（语言高亮覆盖）与 52②（弹窗层 48dp 提档），共五项。
为什么破「≤3 项」的例见「版本策略」，此处不重复。

**发布拆分**（七件事不攒到一个 tag，按阶段分三次发）：

| 发布 | 内容 | 到这一步用户拿到什么 |
| --- | --- | --- |
| **v1.5.0** | 阶段 0–3 + 51 + 52② | 扫码连上、浏览与编辑电脑上的文件、看到桌面正打开的标签 —— 主题已经成立，可发 |
| **v1.5.1** | 阶段 4–5（实时更新 + 离线队列） | 断网不丢内容。条目 44 是本版风险最高项，独立一轮，别拖住前三阶段 |
| **v1.5.2** | 阶段 6（未保存内容移交） | 依赖最浅，单独发 |

**五条待确认全部收敛**（原设计稿 §12.1，现移入其 §12 决策记录）：

1. **开关状态跨重启记忆，启动时上次为开则自动 bind**（2026-09-23 由用户定，**推翻**设计稿 §2.7 的
   「每次启动都是关」倾向）。配套两条硬要求：共享处于开启态时桌面必须有**常驻可见**的状态
   （不只是设置页里那个开关），且**退出应用即释放端口**。
   「不引入常驻后台」这句定位据此收窄定义 = **不注册开机自启、不起后台服务进程**，
   不等于「不记忆开关」；README 与「明确不做」里凡引用这句的地方都要按此措辞，别留下自相矛盾。
2. **可配对多台、同一时刻只服务一台**：第二台连上时桌面提示「已有设备连接中」，手机侧同样给明确
   状态，不排队、不静默等待。冲突矩阵只维护一套。
3. **默认端口 47123**，设置里可改；被占用时**明确报错并给出改端口入口**，不允许静默换端口
   （静默换端口会让手机记着的地址失效，比报错更难查）。
4. **设备名**：桌面取 `COMPUTERNAME`，手机取 `Build.MODEL`（经既有 `HeidBridge` 上报，不新增桥）；
   两端都允许改一个本地备注名，备注名只存在本机、不参与协议。
5. **二维码带桌面长期公钥指纹**（截 8 字节、base32 显示）：手机扫码即校验对端，防同网段抢答。
   密度代价有实测底：v1.4.2 条目 49 已验过 140 CSS px 显示尺寸下 jsQR 能解，本码长度需与之同量级，
   超了就把指纹挪到配对握手阶段校验（第二条兜底，不改协议）。

**加密的落点改了，且是省事的改法**：设计稿 §2.8 写的是新增 `chacha20poly1305` + `hkdf` 两个 crate。
实测 `Cargo.lock` 里 **`ring 0.17.14` 已随 `ureq → rustls 0.23` 编进 APK**（`aws-lc` 零命中，
rustls 用的就是 ring 后端），而 `ring::aead::CHACHA20_POLY1305` / `ring::hkdf` / `ring::agreement::X25519`
三样齐全 —— **整条加密链零新增依赖**。原方案的两个 crate 不加。

### 开工前的否决性实测（不写业务代码就能做完，做完才允许动阶段 0）

1. ✅ **release APK 能走局域网明文 TCP —— 已验，通**（2026-09-23，全量证据在设计稿 §11.2）。
   实测：Tab S8 上 **release 签名的 1.4.1 包**经「从网址导入」发起 `http_get`，本机 `0.0.0.0:8765`
   收到 `GET /probe-x ua="ureq/2.12.1"`。UA 是 ureq 而非 Chromium → 走的是 **Rust 原生 socket**；
   8765 不在任何明文白名单内 → **`usesCleartextTraffic="false"` 不约束 `std::net`**。
   为什么这条当初是否决性的：v1.5 手机主动连桌面，若明文限制是 **UID 级**（内核 eBPF 丢包），
   `std::net` 一样连不出去，七件事全部要重排 —— 现在不用重排。三条兜底（挑白名单端口 / 上 rustls TLS /
   放开 cleartext）全部不需要，留档备查。控制组同日早先也过了：`uid=shell` 的 `nc` 连同端口拿 200。
   **同一轮还排掉了一种误读**：那次导入最后落地的标签页是 `net::ERR_CLEARTEXT_NOT_PERMITTED`
   （静态抓取已成功，因正文过薄走了「内置浏览器渲染」兜底，那条走 WebView 被挡）——
   即**同一进程内 Rust 放行、WebView 拦截**，不是「这台设备压根不执行明文策略」。
   **覆盖范围要说清**：测的是「手机作为客户端连出」，正是手机侧唯一的用法；
   **桌面侧 bind 的可达性不在这条里**（见下第 2 条，仍未验）。
   - 顺带一条会反复用到的方法账：release 包**没有 CDP**，手机上定位小控件只能靠截图，而**截图给到
     模型时会被缩放** —— 本轮前三次「点了没反应」全是把没真看过的裁片坐标当成实测值用（`input tap`
     落在遮罩上把弹窗关了），第四次「裁 ≤1100px 宽 + 亲眼读 + 换算 crop 偏移」才命中。
     以后每个阶段的设备实测都要按这条做，否则又会烧掉十几轮假阴性。
2. **首次 bind 的防火墙体验**（设计稿 §11.1）—— **这台机器测不出用户侧结论，别再拿它当证据**：
   本机 WLAN 是 **Public** 配置文件，但 `Get-NetFirewallRule -DisplayName '*node*'` 实测有四条
   Inbound/Allow（`Profiles` 含 4=Public），是以往开发留下的放行规则。所以「PC 起监听、平板连得上」
   在这台机器上证明不了任何用户会遇到的事。真验必须用**应用自己 bind**。
   由此得出一条实现要求：桌面侧 bind 成功但 N 秒内没有任何连接尝试时，要提示「可能被 Windows 防火墙
   拦截」并给放行步骤 —— 半死状态（端口开着、包进不来）比直接报错难查得多。
3. ✅ **手机 WebView 开摄像头解二维码 —— 已两台实测（2026-09-23，扫码器已落地）**。
   已查明的一半：Tauri 生成的 `RustWebChromeClient.onPermissionRequest`（`gen/android/app/src/main/java/
   com/nexus/editor/generated/RustWebChromeClient.kt:96-119`）**已实现** `VIDEO_CAPTURE` → 运行时申请
   `CAMERA` 并 grant/deny，所以**不需要新写 Kotlin 桥**，只要 manifest 补
   `<uses-permission android:name="android.permission.CAMERA" />`（已补）。
   三半的实测结论（Tab S8 + Mate 40 Pro 真机）：
   ① `http://tauri.localhost` 下 `getUserMedia` **两台都能拿到流** → 安全上下文成立，**不必改 `useHttpsScheme`**；
   ② Mate 40 Pro 的 `BarcodeDetector` **对象存在但盲检**（`detect()` 一直返回空）→ **jsQR 懒加载兜底是必需的**，
      并启后能扫上；Tab S8 原生 `BarcodeDetector` 可用；
   ③ 预览用 `bg-black` 实底、不依赖 `backdrop-filter`，两台正常。
   **当时的遗留「切换摄像头手感」已于 2026-09-24 本轮重做收口**（一次开流 + 冻结帧平滑切换 + 对数捏合变焦
   + 占用退避重试），Tab S8 已实测；Mate 40 Pro 与「黑闪/跟手」的人眼确认仍开着 ——
   全量记在 `docs/bugs/2026-09-23-scanner-camera-switch-ux.md`。

### 每阶段的验收（设计稿 §14 的通用门禁之外，本阶段特有的一条）

| 阶段 | 交付判据 | 这台机器上怎么量 |
| --- | --- | --- |
| 0 ✅ | 两端 hello 成功；抓包看不到明文 —— **2026-09-23 已达** | `cargo test --lib link::`（21 例）+ `npx vitest run src/lib/link.test.ts`（11 例）+ `node scripts/link-verify.mjs`（18 项运行时检查，要先起带 CDP 的 dev）。该脚本**用 Node 的 crypto 独立实现了一遍对端协议**、分别扮演手机与桌面，所以"能互通"不是 Rust 自己跟自己通 |
| 1 ✅🔧 | **免扫重连核心已完成并实测（2026-09-23）**：桌面二维码（带一次性票 + 6 位短码 + 长期身份指纹）、TOFU 确认、配对后双方各存一把从已认证 ECDH 派生的 LS、免扫自动重连、多配对单在线、票用后即废。**应用内相机扫码器也已落地并两台实测能扫上**（§7.2 三点全过，见上）。扫码器手感已于 2026-09-24 重做收口：
一次开流（旧版为拿权限多开一枪）、扫码定为**单镜头**（用户主动换镜头才走冻结帧过渡）、
捏合改对数映射 + 每帧低通下发、
无硬件变焦的镜头由数码放大顶上（解码只喂放大后的可见区域）、占用按退避重试不再误报「权限被拒」，
并修掉预览被设置弹窗 `backdrop-blur` 关成一格（平板上非全屏）的真 bug。**已两台复测通过**
（2026-09-24：黑闪与卡顿随「换镜头」动作一并消失，Mate 靠 8K 交付尺寸选中主摄）；
捏合判定「够用、谈不上很流畅、扫码够了」，不再花一轮 → `docs/bugs/2026-09-23-scanner-camera-switch-ux.md` | `cargo test --lib link::`（38 例，含两端 TCP 真实往返）+ `node scripts/link-verify.mjs`（阶段 1 全绿：Node 独立实现验出桌面 Ed25519 身份签名 + 二维码指纹、LS 对称、免扫重连、票作废）+ 两台真机扫一扫实测（Tab S8 原生 BarcodeDetector、Mate 走 jsQR 兜底，均能扫上配对）+ `npx vitest run src/lib/scanCam.test.ts src/components/QrScanner.camera.test.tsx`（36 例：变焦数学、一次开流、选主摄、占用/权限分流、探测期不给看画面、捏合接线） |
| 2 树同步 + 读写 ✅🔧 | **已完成（2026-09-24）**：`roots::resolve_rel` 四组逃逸用例先写失败再实现、`list`/`stat`/`read`/`write` 走已实测的 AEAD 会话、`hide-remote://` 前端全链路、设置面板「共享范围明示 + 防火墙提示」。**真机那一项没做**：本机两个 AVD 都没有网络路由（下详） | `cargo test --lib link::`（**64 例**：roots 10 + fsrv 16 + 帧/握手既有 38，含 `远程命令走完整加密链路往返` 那条双端真实 TCP）+ `npx vitest run`（**1018 例**，含 `remote.test.ts` 48、`remoteTree.test.ts` 8、`FileTreeSidebar.remote.test.tsx` 5）+ `node scripts/link-verify.mjs`（**58 项 PASS / 0 FAIL**，其中 F 段 29 项是**跑着的应用**当服务端、逐条与磁盘核对；G 段 8 项是应用当客户端发远程命令）|
| 3 标签同步 ✅🔧 | **已完成（2026-09-24）**：桌面各窗口把本窗口的根与标签报进进程级看板、按 Tauri 焦点事件取「聚焦窗口那份」、`tabs` 命令 + `@w/<id>/<名字>` 根外白名单（**每次访问重新 canonicalize**）、`event {type:'tabs'}` 推送、手机端「电脑上正打开的」列表（脏标签由**服务端**留空引用而不只是手机置灰）。**没做**：真机局域网往返（本机 AVD 无网络路由，见阶段 2 那条） | `cargo test --lib link::`（**84 例**：board 15 + fsrv 19 + roots 10 + 帧/握手/配对既有，含 `标签白名单与推送走完整加密链路` 那条真实 TCP：tabs→@w 读→无请求在飞时收到 event→关标签后同一条引用当场 `notopen`）+ `npx vitest run`（**84 文件 / 1040 例**，新增 `useTabReport.test.tsx` 7、`RemoteTabsSheet.test.tsx` 6、`remote.test.ts` 52（含 @w 段 4））+ `node scripts/link-verify.mjs`（**74 项 PASS / 0 FAIL**，其中 H 段 14 项跑在**真实应用**上：真实点文件树开标签、多开一个文档窗口验「聚焦窗口那份」的判定、换根后同一标签改由 `@w` 寻址并在手机上读到逐字内容）|
| 4 实时更新 ✅🔧 | 桌面改文件，手机秒级跟上 | **已完成（2026-09-24）**：监听随连接而生死、变更按 300 ms 定周期合并成只列目录的 `event{fs}`、换根推 `rootChanged`；手机端只重列受影响的那几层，打开中的文件走既有 diff 时间线。`cargo test --lib link::` 94 例 + `npx vitest run` 1068 例 + `node scripts/link-verify.mjs` **87 项 PASS / 0 FAIL**（I 段 13 项跑在真实应用上：node_modules 里 40 次写入一帧都不产生）。**跨机真机往返与帧延迟 2026-09-25 已量到**：桌面 = 另一台电脑、共享根 = 那台机的本地目录，5/5 到帧、首帧 54–82 ms；本机 dev 桌面 + 同一连接内 5 轮 5/5、首帧 26 ms。第一次跨机量出的「1/5」是判据脚本把 `transformCallback` 的 `once` 当成 persistent，见上面开工单第 1 条 |
| 5 离线队列 ✅🔧 | 拔网线编辑不丢，恢复后回写或明确请确认 | **真关 WiFi**，不用代码模拟；进程被杀用「设置里强行停止」再开。**已完成（2026-09-25）**：IndexedDB 队列 + 折叠（内容最新、基线取最初那份）+ 按 `queuedAt` 回放 + 五态里的三态标记与树顶横幅 + 长按/右键同一串动作；回放改为直接 `write`（桌面本来就判过一次）。模拟器实测：断连保存 1 秒落队列、`am force-stop` 冷启动后还在、免扫重连后一致的那份落到桌面、桌面也改过的转红点且不覆盖。全量与两条欠账见「阶段 5 收口记录」 |
| 6 内容移交 | 无路径标签能搬到手机继续写 | 两端各验一次方向 |

两台真机都是 **release 签名**（Tab S8 = 1.4.1、Mate 40 Pro = 1.4.2），本地 release 包可 `install -r`
原地升级、保留 SAF 授权；换 debug 包要卸载，会清掉授权 —— 阶段 0 起就固定用 release 包测，
顺带把 §11.2 那条一直验到发版。装机统一走 `scripts/android-install.sh`（`install-only` 跳过构建）。

### 阶段 2 收口记录（2026-09-24）与三条按实测改掉的口径

**先做三条低成本收尾**（都属于阶段 1，别带着它们开工下一阶段）：

1. ~~扫码器灰色播放按钮占位符定性~~ —— Tab S8 复测已不见，最可能就是选错镜头那段时间的产物。
   若再现：进扫一扫点底部调试行展开日志环缓冲，看 `play(attach) 成功/被拒` 一行即可判定
   （logcat 在华为上不可靠，只能走界面这份）。
2. ~~切换黑闪与卡顿~~ —— **已结**（2026-09-24 用户复测两台均无，并给出因果：症状挂在「换镜头」
   这个动作上，扫码改成单镜头后动作消失，症状随之消失）；**捏合跟手**也已判定
   「够用、谈不上很流畅、扫码够了」，不再花一轮。三条收尾全部结清，直接进阶段 2。
3. ~~Tab S8 补机型档案种子~~ —— Tab S8 只有两颗后置、探测本来就便宜，复测无问题，不必补。

**交付**：`src-tauri/src/link/roots.rs`（`resolve_rel` + `is_within`）、`link/fsrv.rs`（四条命令，
不碰网络也不碰会话）、`link.rs` 的 `Msg::Req/Res` + `run_pump` 按角色分派 + `Conn`
（发出队列 / 待应答表 / Condvar 唤醒）、命令 `link_set_root` / `link_request`；前端 `lib/remote.ts`
（身份键编解码 + 稳定码分流）、`lib/remoteTree.ts`（`DirLister` 第三实现 + **按根选路** `pickLister`）、
以及 `platform.ts` / `fileIO.ts` / `largeFile.ts` / `useFileActions.ts` / `FileTreeSidebar.tsx` / `App.tsx` 的分支。

三条与设计稿字面不一致的地方，都是实现时按「一件事只留一个口径」改的，设计稿不再回头改：

1. **换行符不进协议**（§4.2 的 `read` 载荷原本有 `eol`）。桌面侧 eol 的权威判据本就是前端的
   `detectLineEnding` + 落盘前 `applyLineEnding`，远程读取又走同一个 `openedFromDecoded`——
   服务端再算一份只会多出第二个口径，两份还可能不一致。所以 `read` 原样带回 CRLF，归一交给前端；
   `write` 收到的 `text` 已是调用方按 eol 还原过的最终字节。
2. **基线只用内容哈希，不带 `baseMtimeMs`**（§4.2/§5.4 写的是「hash + mtimeMs」）。
   mtime 判等在桌面刚保存过的那一秒内分不出新旧，拿它判等会**静默覆盖桌面的改动**；内容哈希不会。
   `mtimeMs` 仍在 `stat`/`read` 里带回，只用于展示。基线为空串的写入直接以 `badparams` 拒——
   没有基线就落盘等于毁数据。
3. **远程上限 6MB 而不是 32MB**。32MB 是桌面「只读分块预览」那条线，而 §5.3 明确不做远程分块预览，
   加上单帧 8MB 上限，超 6MB 的文件即使传过来也只能进一个既不能编辑又不能分窗读的死角
   （6MB = 编辑器 200 万字符可编辑线在 CJK 下的最坏字节数）。`stat` 照实报尺寸，`read` 以 `toobig` 拒。

**本轮查出的两条实现级事实**（都会反复用到）：

- **加一条 `#[tauri::command]` 要同时改四处**：`link.rs` 实现、`lib.rs` 的 `generate_handler!`、
  `build.rs` 的命令清单、`capabilities/default.json` 的 `allow-<命令名>`（`build.rs` 头部注释早已写明，
  本轮仍然漏了第三、四处）。只漏 `build.rs` 时它会 panic 并打印全部已知权限（很长，容易误读成 ACL 语法错）；
  **只漏 `capabilities` 时没有任何编译错误**，运行时表现为「link_set_root not allowed. Command not found」。
  是 `scripts/link-verify.mjs` 的 F 段把它当失败暴露出来的——这类「命令注册了但没授权」单测查不出来。
- **同一端口关共享后短时间内绑不回来**：`link_server_stop` 确实释放了监听（实测 `netstat` 无 LISTENING 残留，
  满足「关闭即释放端口」），但上一台设备的连接留在 TIME_WAIT 时，std 的监听套接字按
  `SO_EXCLUSIVEADDRUSE` 绑定会被直接拒——**实测 45 秒未散**，短重试救不了。
  按决策 §12.1-3「不许静默换端口」办成可操作文案（`bind_listener`）：说清是端口被占、等一分钟或换一个端口。

**验收判据里没做到的那一条**：「真机在共享根里打开一个 md、编辑、保存回桌面」——
本机两个 AVD（`phone35`、`aurora35`）客户机**完全没有网络路由**（`ping 10.0.2.2` 与 `ping 8.8.8.8`
都是 `connect: Network is unreachable`），拿它们跑局域网等于什么都没验，所以这一条**没有**在本轮冒充通过。
模拟器上做了两件与网络无关且确实会翻车的事：x86_64 debug 包装进 `aurora35`，① 页面入口脚本哈希
== 本轮 `dist/index.html` 的哈希（装的是这一版前端）；② `link_request` 在安卓侧回的是
「手机上还没有连着桌面」而**不是**「not allowed」，`link_set_root` 同样被授权——即上面那条四处登记的坑
在 APK 这一侧也过掉了。**真实 Android 客户端的局域网往返仍未验过**，留给阶段 3 连着桌面标签同步一起上真机。

### 阶段 3 收口记录（2026-09-24）：四条按实测改掉的口径与四条真 bug

**交付**：`link/board.rs`（进程级看板 + 根外白名单 + `@w/<id>/<名字>` 引用解析）、`link/board_tests.rs`
（15 例，四组安全用例**先写失败再实现**）、`fsrv` 的 `Scope` 化与 `tabs` 命令、`Msg::Event`
与服务端推送队列 `Push`、`link_report_tabs` 命令 + 四处登记、`lib.rs` 全局 `on_window_event`
（Focused / Destroyed）、前端 `hooks/useTabReport.ts`（节流 + 按可见变化去重）、`remote.ts` 的 @w 分支、
手机端 `RemoteTabsSheet.tsx`（portal 到 body、脏标签置灰）。

四条与设计稿字面不一致的地方，都是实现时按「一件事只留一个口径」改的：

1. **`tabOpen {index}` 取消，改用 `@w/<id>/<名字>` 引用**（§4.2 命令集里那条）。下标随开关标签漂移，
   手机上那份标签的身份键必须稳定；更要紧的是：按 index 开就得为它另写一套读/写/冲突判定，
   而 `@w` 引用能直接塞进 `hide-remote://` 的 rel 位，与根内文件**共用同一条通道**（含基线哈希与冲突）。
2. **白名单取所有活窗口的并集，列表只取聚焦窗口那一份**。设计稿 §6.1 只说「tabs 命令返回聚焦窗口那份」，
   若白名单也跟着只取聚焦窗口，用户在桌面 alt-tab 一下就把手机上**正在编辑**的那份文件抽走了。
3. **共享根改成按窗口存**（阶段 2 是全局一份）。多窗口下「最后挂载的窗口赢」是错的：
   手机看到的标签列表来自聚焦窗口，树却可能是另一台窗口的根 —— 两者必须同一份判定。
   连带效果：某窗口没开文件树 → 该窗口聚焦时只暴露标签（正是 §2 决策 3 的「没开树就只给标签」）。
4. **「根外额外暴露」只数根外**。白名单条目里根内根外混在一起（引用不能随聚焦窗口漂移），
   但设置面板那行说的是「根外多交出去几个」，含根内就虚报。

四条在真机之外被这轮实测查出来的真 bug（都已修，各自有回归用例）：

- **新开一个文档窗口会把手机上正连着的链路踢掉**：每个窗口挂载都跑一次「上次开着共享就自动 bind」，
  而 `link_server_start` 非幂等 —— 它 `replace_stop` 掉正在跑的那台。修法两条：自动开只由主窗口发起；
  `link_server_start` 对「同端口且已在监听」直接回当前状态。这条只有多窗口才会暴露，
  也正是「多开两个窗口才能验判定」这条判据的价值。
- **关窗后迟到的上报会把死窗口复活**：关窗流程会先收干净标签，那次上报可能落在 `Destroyed` 之后，
  于是看板里又有了那个窗口，还成了「最近动过的」—— 手机上凭空多出一份空列表。
  窗口存在与否只有 Tauri 知道（`window_alive`），不能在看板里记死标签：`win-N` 是**复用**的标签。
- **`PREFS_KEY` 里混进一个 U+2026 省略号**（`heid-l…refs`）：读写同一个常量所以功能看着是好的，
  但 `scripts/link-verify.mjs` G 段那句「先清掉 `heid-link-prefs.enabled`」一直在清一个没人读的键 ——
  那条防护从写下来就没生效过。H 段第一次跑到「reload 后服务端换到 47123」就是这个原因。
  代价：这台机器上「记忆开着共享」的状态因键名修正而回到关，需要重新打开一次开关。
- **`document.hasFocus()` 不能当窗口聚焦判据**：WebView2 在窗口失活后仍报告文档有焦点
  （实测：手机看到的明明是新建窗口的列表，主窗口 hasFocus 仍是 true）。改用 `plugin:window|is_focused`。
  另外 `set_focus` 没有被授权（也不该为测试授权），所以「主动切焦点」这条路在 H 段是走不通的。

**验收做到哪、哪一条没做**：`cargo test --lib link::` 84 例 + `npx vitest run` 84 文件 / 1040 例 +
`node scripts/link-verify.mjs` 74 项 PASS / 0 FAIL（H 段 14 项跑在真实应用上，含多开一个文档窗口的
聚焦判定、换根后同一标签改由 `@w` 寻址并在手机上读到逐字内容）。**没做到的一条**：
「真机在共享根里打开 md、编辑、保存回桌面」以及本轮的「手机看桌面标签并接着改」——
本机两个 AVD 都没有网络路由（阶段 2 那条结论仍然有效），而用户明确说本轮不必折腾真机，
所以**真实 Android 客户端的局域网往返仍未验过**，留给阶段 4 与实时更新一起上真机（届时由用户配合）。
H 段的聚焦判定在「两个窗口都不在前台」时会降级为「新建即切换 + 关窗回落」这一对，
降级时脚本会如实打一行说明，不会冒充成了字面判据。

### 阶段 4 前置量测（2026-09-24）：三件事的数字，与由数字改掉的三条口径

判据是表格那格写的「拿本仓当共享根，先量事件量与去抖」。探针 = `src-tauri/src/link/watch_probe.rs`
（`#[ignore]`，不进 CI），它跑的就是生产那一份 `link::watch::Batch`，所以下面这些数字不是估的。
共享根都是本仓（13.4 万文件、504 MB 的 `node_modules`），合并窗口 300 ms 定周期：

| 场景 | 原始事件 / 时长 | 平均 · 峰值 条/秒 | 过逐层黑名单后剩 | 帧数（退化数） | 峰值 帧/秒 · 字节/秒 | 字节合计 | 单帧最多目录 | 合并累计耗时 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `npm install` 形状：13.4 万文件写进 `根/probe-churn/node_modules` | 156,664 / 11.0 s | 13,055 · 15,688 | **264 条 = 0.17%** | 1（0） | 1 · 24 | **24 B** | 1 | 0 ms |
| 可见目录里的同一场风暴：13.4 万文件写进 `根/probe-stress` | 156,699 / 11.1 s | 13,058 · 16,443 | 83,049 = 53.0% | 27（0） | 4 · 14,274 | 70,802 B | **205** | 1 ms |
| 高速删掉两棵这样的子树（`robocopy /MIR /MT:16` + `rm -rf`） | 150,956 / 29.4 s | 5,032 · 6,351 | 45,460 = 30.1% | 74（0） | 4 · 8,185 | 55,291 B | 93 | 0 ms |
| 日常一次保存（改 2 个 md + 建/改/改名/删一棵三层小目录树） | 21 / 3.2 s | 7 · 9 | 20 = 95.2% | 3（0） | 2 · 83 | 129 B | 4 | 0 ms |
| **对照**：`npm install` 那一格完全不过滤 | 同上 | 同上 | 100% | 37 | — | 144,864 B | 269（最大一帧 13,349 B） | — |
| **对照**：可见目录那一格完全不过滤 | 同上 | 同上 | 100% | 37 | — | 110,756 B | 253（最大一帧 9,274 B） | — |
| **被否掉的写法**：载荷带文件清单，为分辨文件还是目录逐条 `stat` | 156,769 / 12.5 s | 12,059 · 15,837 | 83,118 = 53.0% | 29（**16 帧撞上限退化**） | 3 · 53,584 | 279,717 B | — | **最慢一帧 364.8 ms、全程 3,801 ms** |

三条按实测改掉的口径：

1. **链路撑得住，但靠的是两个机制各管一段，缺一个就被刷爆：黑名单管"依赖树在共享根里"，
   目录去重管其余**。`npm install` 形状过完逐层黑名单只剩 0.17%（264 条），整段推 **24 字节**；
   而用户把一棵不含黑名单目录的大树当共享根时，黑名单一点忙都帮不上（保留 53%，与不过滤的对照行
   只差 1.6×），那一格是**目录去重**把 15.7 万条事件压成 27 帧 / 70 KB / 峰值 4 帧每秒 ——
   相对单帧 8 MB 的上限与一次 6 MB 的文件读取，这不算瓶颈。**只判第一层也不行**：
   `npm install` 那格的第一层是共享根自己的目录名，只有逐层判才让子树在 `node_modules` 那一层被滤掉
   （只判第一层，保留比例从 0.17% 变 53%）。
2. **载荷只列「哪些目录的内容变了」，不列文件**（工单原文写的是 `changes:[…]` 文件清单）。
   表格最后一行就是按工单字面写完再量的结果，三条代价都在那一行：体积是目录版的 4 倍、
   29 帧里 16 帧撞条目上限被迫退化、以及为了分辨一个名字是文件还是目录（Windows 的通知不带类型）
   逐条 `stat` —— 一次合并 364.8 ms 里 stat 占 364.8 ms、序列化只占 0.1 ms，全程 3.8 秒。
   而手机端要做的两件事都不需要文件清单：树按「展开着的层在不在 dirs 里」重列，
   「我打开着的这个文件被桌面改了没」按「它的父目录在不在 dirs 里」判，命中再去 `stat` 比哈希，
   与阶段 2 的写冲突同一套判据（**内容哈希才是权威，事件只是「该去看一眼」的提示**）。
   代价如实记：手机端要多跑一次 `stat` 才知道是不是自己正看着的那个文件。
   上限因此按目录数定：实测单帧峰值 205 → `MAX_PUSH_DIRS = 256`、字节上限 32 KB，
   超了退化成不带载荷的 `event{type:'fs'}`（阶段 3 已有的「变了，去重取」语义）。
   换成只推目录之后**四个场景一帧都不必退化**。
3. **合并用定周期 flush（300 ms），不用拖尾去抖**。持续变更的那三格，拖尾式**全部只发 1 帧**
   而变更持续 11~29 秒 —— 事件不间断时它永远等不到那 300 ms 的安静，手机端整段不刷新，
   与「实时更新」字面相反。日常保存那一格两者都是 3 帧（事件之间本来就有 >300 ms 的间隔），
   也就是说定周期只在持续变更时更好、成本一样，没有理由用拖尾。

顺带量到两件以后还会反复用到的事实：
① **监听自身的成本可忽略** —— 递归 watch 建立 230~310 µs，跑完 15.7 万条事件之后探针进程
   工作集 18 MB、句柄 139 个（notify 在 Windows 上是**一个递归句柄**，不是每目录一个），
   所以「为互联多挂一份监听」与桌面文件树那份并存不冲突。
② **`canonicalize()` 在 Windows 上返回 `\\?\` 前缀路径，通知回来的路径会带着它一起拼** ——
   取相对路径必须按组件处理，不能按字符串前缀（阶段 2 的 `roots.rs` 早写过这条，本轮再撞一次）。
③ 改名实测一次产生 `rename-from` + `rename-to` 两条；「丢 from 只留 to」那条规则在日常保存那格
   被验证（21 条原始 → 20 条送去合并，丢的正是 from）。

**如实记下列到与没量到的**：
① 「`npm install` 形状」是把整份 `node_modules` 拷进 `根/probe-churn/node_modules` 复现的，不是真跑
`npm install`（真跑会改 `package-lock.json`）。文件数、目录深度与路径里的 `node_modules` 成分都与真装一致，
所以「过滤滤掉什么」这一项是按字面场景量的；差别是 npm 自己的临时文件改名与硬链接阶段没覆盖。
② 「可见目录」那两格是压力场景（13.4 万文件的变更放在一个不含黑名单目录的位置），远超本仓日常，
留它是为了定上限，不是为了说日常长这样。
③ **手机端收到帧之后重列的往返成本没量** —— 那是实现阶段的事，届时由 `scripts/link-verify.mjs` 量。
④ 帧的**延迟**这里也量不了（探针记的是事件时间戳）：定周期 flush 的墙上延迟按构造是
合并窗口 300 ms + 泵线程 `POLL` 20 ms + 一次 `list` 往返，真数等真机（工单第 5 条）。

复现：
`HEID_PROBE_ROOT='<仓库绝对路径>' HEID_PROBE_TRIGGER='<仓库>/probe-trigger' cargo test --release --lib watch_probe -- --ignored --nocapture`，
等它打 `READY` 之后 `touch '<仓库>/probe-trigger'` 再制造变更；探针静默 5~8 秒自己收工。
临时目录 `probe-*` 用完删掉（本轮已清，工作区只剩源码与文档改动）。

### 阶段 4 收口记录（2026-09-24）：交付、四条按实测改掉的写法、以及本轮查出的三条真 bug

**交付**：桌面侧 `link/watch.rs`（`Batch`：逐层黑名单 + 链接点按目录缓存 + 定周期合并；
`payload_of`：目录集合 → `{"dirs":[…]}` 与两处上限；`spawn`：随连接而建的递归监听线程）；
`link.rs` 的 `Push::queue_fs` + 出口合并（`fs` 那一类不套「同类型只留一条」，而是在 `take` 时
把攒下的目录并成一帧）、`EVENT_FS` / `EVENT_ROOT`、`sync_fs_watch`（监听随连接与聚焦窗口的根而生灭）、
`board_edit`（把"要不要通知对端"与"根换了没"收在同一次加锁里）；`Cargo.toml` 把 notify 从
dev 依赖提成桌面直接依赖（**不新增编译产物**：8.2.0 已随 tauri-plugin-fs 在树里）。
手机端：`lib/remoteChanges.ts`（事件名、载荷解析、求交判据、订阅入口）、
`FileTreeSidebar`「只重列受影响的那几层」+ 换根重建、`hooks/useRemoteFileChanges.ts`
（打开中的远程文件按"父目录命中 → `stat` 比哈希 → 变了才读"接进**桌面外部修改同一个**
`handleExternalChange`，因此时间线、采纳/忽略、脏标记语义全都沿用既有的那套，没有新造提示）。

四条与工单字面不同、按实测改掉的写法：

1. **载荷只列目录不列文件**（工单写 `changes:[…]`）—— 见上一节第 2 条，实测逐条 `stat`
   一帧 364.8 ms、体积 4 倍、60% 的帧撞上限。
2. **定周期 flush 而不是拖尾去抖** —— 拖尾式在持续变更期间一帧都不发。
3. **「桌面保存一次 = 手机端一帧」不成立**：Windows 把一次 `writeFileSync` 分成创建 / 写数据 /
   关闭时补尺寸几条通知，彼此间隔能越过一个合并窗口，真实应用上实测 3 帧、载荷是同一份
   `{"dirs":[""]}`。所以验收判据写成「个位数帧、载荷种类 ≤2」，
   而「有没有第二份监听在推」交给「换根之后旧根不再推帧」那条去夹 —— 那条才是真判据。
4. **换根推 `rootChanged` 而不是 `tabs`**：它是 `tabs` 的超集（手机端要丢树缓存、重列、再重取列表），
   两个一起推等于让对端为同一件事跑两轮。桌面侧四个看板变更点（上报根、上报标签、聚焦、关窗）
   统一走 `board_edit`，一次锁内同时算出「变了没」和「根换了没」。

**本轮查出的三条真 bug**（前两条已修并有回归用例；第三条修在验收脚本里）：

- **`Push::take` 发完不清空目录集合** → 同一批目录每 20 ms 重发一遍，等于自己把链路刷爆。
  是 `fs_推送按窗口合并载荷且装不下就整体退化` 里那句「发过的不该重复发」先红才发现的。
- **监听建不起来时走了 `mark_error`**：一个只影响实时更新的附属失败（共享根被上一轮删了 /
  没权限 / 路径过长）会把 `connected` 一起打成 false，界面谎报"链路断了"，而读写一条都没受影响。
  现场是全量验收里 A、H 段各两条莫名其妙地红了 —— 关掉监听后 14/14 过，才定位到这里。
  修法：只往 stderr 记一行，不碰状态快照。
- **验收脚本自己的 `frameReader` 超时不摘 waiter**：一个已经 reject 的 waiter 留在队列里，
  下一帧被交给它并就地丢掉 —— 于是"之后所有帧都看不见"。这条把一次**真能收到推送**的链路
  连读了 5 轮都报"0 帧"，白查了三轮过滤器、监听与窗口状态。教训写进了那段代码的注释：
  判据同时含「该来的来了」和「不该来的没来」时，工具自己漏帧会让两类判据一起给出误导性的答案。

**验收做到哪**：`cargo test --lib link::` **94 例**（新增 3 条）+ `cargo test --lib` 168 例；
`npx vitest run` **87 文件 / 1068 例**（新增 28 例：`remoteChanges.test.ts` 11、
`useRemoteFileChanges.test.tsx` 10、`FileTreeSidebar.live.test.tsx` 7）；
`node scripts/link-verify.mjs` **87 项 PASS / 0 FAIL**，其中 I 段 13 项跑在真实应用上：
桌面改文件 → 手机端无请求在飞收到 `event{fs}`、根下报 `""`、子目录报 `src`、
node_modules 里 40 次写入**一帧都不产生**、风暴之后主路还在、换根收到 `rootChanged`
且新根列得出来、新根按新根的相对路径报、旧根不再推帧。
监听没建起来时也不再把链路误报成断开（A、H 段那两条同时复原）。

**没做到的一条**（工单第 5 条，仍然开着）：真实 Android 客户端在局域网里跑通 ①阶段 2 的读写
②本轮的实时更新 ③§11.1 防火墙用户侧形态 —— 本机两个 AVD 都没有网络路由，这三条只能等真机。
帧延迟同样没有实测值：按构造是「合并窗口 300 ms + 泵 `POLL` 20 ms + 一次 `list` 往返」。
另外安卓侧的编译本轮只做了 `cfg(desktop)` 分支的人工核对（`Push` 的 `dirs` 字段、
`queue_fs`、`take` 里那两段、`fs_watch` 字段与 `sync_fs_watch` 的空实现都按 desktop 关好），
**没有真跑一次 `tauri android build`** —— 下一次装真机包时会自然暴露。

复现 I 段：`WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9223 npm run tauri:dev`
起应用，再 `LINK_VERIFY_ONLY=I node scripts/link-verify.mjs`。
**注意**：共享根必须由前端报（脚本换 `localStorage['heid-tree-root']` + reload），
只 invoke `link_set_root` 会被前端下一次上报盖回去 —— 暴露面按聚焦窗口取，谁报的才算数。

### 2026-09-25 收口：配对状态面四条小的（四条全做完并实测）

四条都按工单原判据做完；"当时是怎么定位的"逐条写在
`docs/bugs/2026-09-25-link-lan-findings.md`，这里只留结论与验收数字。

1. ✅ **(a) 归因不是写反，是被冲掉。** 跑 A 段那对判据现形：过期票那次**确实**先落到
   「配对码已过期或已用过」，随后一条 `nc host port`（连上、一个字都不发、断开）把它改成
   「对端已关闭连接」—— 那条连接连 `read_plain` 都没过去，却和一次真配对尝试共用同一行状态。
   口径改成：`server_handshake` 的失败带 `HandshakeFail.peer_spoke`，**对端至少完整地发来过一帧**
   才有资格写「上次失败」那一行（后面真来一次失败照旧覆盖 —— 那是新的一次尝试）。
2. ✅ **(b) `connected` 押到对端第一帧。** 桌面进 `run_pump` 立刻发一个 Ping（原来要等 15 s 心跳），
   手机把 `connected` 推到收到第一帧，中间新增一档 `waitingConfirm` → 界面「等待电脑上确认」；
   `run_pump` 另外接住 `Refused`，桌面拒绝的原话因此能落到手机上（原来它掉进"不支持的消息"）。
   连带成本照工单落地：`link-verify` B/G 段里 Node 扮桌面也补那一下 Ping（`ctl.serve()`）——
   判据跟着协议走，不是给测试开后门。
3. ✅ **(c) 桌面名走 `Auth.name`**（工单选的那条：免扫重连路上没有配对码可查）。
   `ClientIntent` 里那个 `name` 随之删掉，配对码里的 `name` 不再是显示名来源。
4. ✅ **(d) `toobig` 那句不再自己算尺寸**：病灶就是 `fsrv.rs` 里那个整除。尺寸只有一个口径
   （前端 `largeFile.formatBytes`），数字走 `stat`，带数字的那句是手机端 `remote.errTooLarge`
   （打开路径上的预检，阶段 2 就在那儿、本批没动）。两侧各钉一条：Rust 用例断言服务端那句里
   不出现任何数字，`remote.test.ts` 断言 `formatBytes` 把 7,000,000 与上限算成「6.7 MB / 6.0 MB」。
   **真机实测**（aurora35 点共享根里那个 7,000,000 字节的 `blob.dat`）：
   弹的就是「这个文件 6.7 MB，超过手机可远程打开的上限 6.0 MB」，两个数不再撞在一起。

**本批验收**：`cargo test --lib link::` **101 例**（新增 3：归因分类 / 桌面名到手机 / 超限文案不报尺寸）
+ `cargo test --lib` 175 例 + `npx vitest run` **90 文件 / 1086 例** + `node scripts/link-verify.mjs`
A–J **105 项 PASS / 0 FAIL**（A 段 +3 条：票过期时那行是什么、空连接不许改它、桌面开始服务即发 ping；
B 段重写成时机判据：握手后不谎报 / 1 s 内转已连接 / 拒绝拿到原话 / 中间档就叫得出电脑名）
+ **真 TOFU 一轮**（普通票、测试档关着、桌面点真按钮）：`aurora35` 本批 x86_64 debug 包 ↔ 本机 dev 桌面，
中档→已连接 231 ms（同钟量在 B 段是 139 ms）、拒绝那次手机拿到「桌面端拒绝了这次配对」、
`peerDevice=MISAKIMIKU` 从中档一路带到已连接。工具：
`node scripts/link-lan-check.mjs tofu <6位短码> --host=IP --serial=emulator-5554`。

**本批顺手收掉的一处工具债**（会反复用到）：`link-verify` 的读取器现在**一条连接只在握手时建一次**、
由 `phoneHandshake` 带出去（`pair.next`）。桌面一开始服务就先推一帧之后，
"一次 data 事件只取一帧、其余字节被扔掉"的裸 `readFrame` 必然把后续帧读丢
（F 段那次 `Unsupported state or unable to authenticate data` 就是这么来的）——
与阶段 4 那条「超时不摘 waiter」同族：**判据工具自己漏帧时，两侧判据会一起给出错答案**。
另一条同类的新账：手机上验"中间那一档"不能靠轮询 `link_status`（它只活几百毫秒，第一版就是这么
误判成 FAIL 的），要订 `heid-link` 事件流；而界面上的文案得**下一帧**再读，
回调里同步取 `innerText` 拿到的是上一份状态渲染出来的界面。

### 阶段 5 收口记录（2026-09-25）：交付、三条按实测改掉的口径、以及真机量到的数

**交付**。前端：`lib/idb.ts`（IndexedDB 极薄封装，项目内第一次；配额/没有存储/事务中止归成
三种原因，失败一律 reject 不静默）、`lib/offlineQueue.ts`（条目形状 + 折叠/顺序/容量三组纯逻辑
+ 读写面，键就是「哪台设备的哪个文件」所以折叠是一次覆盖而不是先查后写）、
`hooks/useOfflineSync.ts`（启动读回、连上回放、进度与取消、销账、给树的标记表）、
`lib/remote.ts` 的 `LINK_DOWN_CODES` + `isLinkDown`、`fileIO.saveFileToDisk` 的 `remoteOffline` 出口、
`useFileActions.persistTab` 的入队与"当场又存成功就销账"、`FileTreeSidebar` 的三态标记 + 树顶横幅 +
右键/长按同一串菜单项、i18n 双语各 26 条。Rust 两条：链路级失败上稳定码
（`nolink` / `dropped` / `timeout`，`link.rs`），`write` 的冲突回包在桌面那份超过远程上限时不带正文
（`fsrv.rs`，见下「查出来的三条」第 3 条）。

四条与设计稿/工单字面不一致的地方，都是实现时按实测改的：

1. **回放不再先 `stat`**（§9.3 第 2 条写的是「每条先 stat 取桌面 hash 再比」）。桌面在 `write` 里
   本来就用基线判过一次、不一致时一个字都不写并把它那一份带回来 —— 手机先 `stat` 一遍是养第二份
   会漂移的判据，多一次往返，且两次之间桌面随时能改。现在一条一次 `write`，冲突时正文一起回来。
2. **条目里不存 `eol`**（§9.1 的载荷原本有）。队列存的是**落盘态文本**（换行符已按标签还原），
   回放原样发出。与阶段 2「换行符不进协议、权威在前端那一份」同一条口径，两处都存就有两份。
3. **`baseMtimeMs` 不进条目**：沿用阶段 2 收口那条（基线只用内容哈希），条目 44 那句是设计稿时期的写法。
4. **永久失败不新增第六态**：桌面按路径拒掉（`outside` / `notopen` / `toobig` …）时条目留在 `queued`、
   `attempts++`、`lastError` 记下原话，下次重连再试；只有「桌面也改过」才转 `conflict`。
   五态表因此原样保留。上限因此按"拒几次都还在"来定，而不是按状态数量定。

**真机量到的**（本机 dev 桌面 ↔ `emulator-5554`，x86_64 debug 包；WiFi 用 `svc wifi disable`
真断、进程用 `am force-stop` 真杀，全量在 `docs/bugs/2026-09-25-stage5-offline-findings.md`）：

- 断连后点保存 **1 秒**出「已离线保存到手机，等待同步」，队列 1 条，标签保持脏、内容不动；
  快是因为 socket 当场报 `os error 103`，泵的 `shutdown` 把在飞的请求全唤醒成 `dropped:`；
  30 秒那一档留给「对端静默但不报错」（合盖 / 信号边缘），两种码都在名单里；
- **强行停止 + 冷启动**之后队列还在（基线 `6232f43c` 一路没变）；
- 免扫重连之后：桌面没动过 → 手机上那行原样落到桌面文件、队列清空；
  桌面也改过 → 转红点 + 横幅「有 1 处需要你确认」，**桌面那份没被覆盖**（两条分支各跑通一次，
  冲突那一支跑了两次，第二次是在加了「另一台电脑」提示之后的包上）；
- 折叠在设备上的形状：同一条 `conflict` 上再离线保存一次 → `queued`、字符 226→280、
  基线与 `attempts` 都保留旧的那份；
- 平板壳（1280 CSS px / dpr 2）：横幅按钮「去连接」**72×48dp**，标记与脏点不叠成两颗。

**这一轮查出来的三条**：

1. **重新配对（不是免扫重连）会让之前攒的条目永久卡住、而且完全静默**。`peerKeyId` 是那把 LS 的哈希，
   每次配对都重新生成临时 X25519 → keyId 换（实测同一台电脑两次配对得到
   `b8e5535d…` 与 `4f7ca381…`）。判据要保留（连着 A 不能把 B 上攒的改动写进 A 的同名文件），
   改的是不许默默卡住：一连上就出一条「有 N 份是攒在另一台电脑上的」，说清"连回那台会自动补上"，
   同一趟连接只提一次。**欠账**：要做成"换键也认得同一台"，得把 `hide-remote://` 的 deviceId
   从 LS 哈希换成长期身份指纹（§12.1-5 那个 fp），那会牵动已打开标签的身份键，单独一轮。
2. **冲突但标签没开着时，「查看差异」会开到一个空弹窗**：时间线的存活域是"被标签引用的路径"，
   重启之后远程标签恢复不了，那条红点就没有对应的差异可看。现在那句先说下一步
   （「打开它之后，在时间线里逐条采纳」），不开一个看起来像坏掉的空窗。
3. **`write` 的冲突回包原先无条件带上桌面正文**：桌面把文件改到 40 MB 之后，手机的一条待回放
   会撑断 8 MB 帧上限、把整条链路打死（后面所有条目一起失败）。现在超限只报"有冲突 + 尺寸 + 哈希"，
   手机据此走「给不出差异」那句，而不是摆一份空的「桌面版本」让人去采纳。
   顺带把二进制判定收成一个口径（`encoding::is_binary` 提为 `pub(crate)`），没在命令面另写一份采样。

**验收做到哪、哪两条没做**：`cargo test --lib link::` **103 例**（新增 2：链路级稳定码 /
冲突超限不带正文）+ `cargo test --lib` 177 例；`npx vitest run` **95 文件 / 1151 例**
（新增 `idb.test.ts`、`offlineQueue.test.ts`、`useOfflineSync.test.tsx`、
`FileTreeSidebar.offline.test.tsx`、`fileIO.remote.test.ts` 五个文件与 `remote.test.ts` 两条码判据）；
`node scripts/link-verify.mjs` A–J **107 项 PASS / 0 FAIL**（G 段 +2 条：断连后的失败必须带稳定码、
未连接那一条必须是 `nolink`）。**没做的两条**：① 412px 手机壳上那条横幅的排版没量（按钮 48dp
是 `pointer-coarse:` 门控、与壳宽无关，但会不会挤成两行没亲眼看过 —— 手机壳的顶栏结构与平板
不是一套 DOM，卡在探测上没继续追）；② 两台真机这一轮没上，端到端是在模拟器上真断 WiFi 验的。

**一条方法账（我自己这轮犯的）**：`cargo test … | tail -8` 的退出码是 `tail` 的，
管道会把编译失败读成"通过"。这一轮的 Rust 门禁就是这么先绿了一次、后来才发现两条
`E0308` 从没跑起来的测试。以后跑任何 `--lib` 都要看 `test result:` 那一行本身。

### 下一会话开工单：阶段 6 与 v1.5.1 发布

1. **阶段 6 未保存内容移交**（条目 44 后半）。设计稿 §10 已经把形状定完：`SessionTab` 增
   `transferred` 变体、复用多窗口 bootstrap 那条「传 `SessionState` → `restoreSessionTabs`」通路、
   光标走既有 `jumpRequest`、单条上限 `MAX_DRAFT_CHARS`、移交语义（源端进"已移交"只读态）而不是同步。
   依赖最浅，单独发（v1.5.2）。
2. **v1.5.1 发布**（阶段 4 + 5）：这一版是「断网不丢内容」那一档，六处版本号 + CI + 签名 +
   发行说明单独成文，并且**每个版本都要走完 `mirror-gitee`**（「获取另一版」的二维码按 tag 直链，
   没走镜像任务就是扫码 404 —— 见条目 49 / 54）。
3. **还欠着的**（别当已收尾）：① 上面第 1 条查出来的 deviceId 换长期指纹；② 手机壳 412px 的
   横幅排版 + 真机（Tab S8 / Mate）走一遍阶段 5 的断网—恢复；③ 阶段 4 记过的
   「共享根是 UNC 网络路径」那一种形态；④ 条目 55（桌面覆盖脏标签的口子）与 57
   （外部打开的标签授权过期后静默丢标签 —— 待办是**别静默**）。
4. **环境账**：AVD 每次开机要 `adb shell cmd wifi connect-network AndroidWifi open` 才有路由；
   `emulator-5554` 上现在装的是本轮 x86_64 debug 包，prefs 里记着 `keyId=b8e5535dfd43a59b`
   （是为了验免扫重连手动写的）；桌面 dev 侧共享与测试配对已在收口时关掉，`link-test/README.txt`
   上留着本轮写进去的三行测试文本。

#### 留档：这四条当时的工单原文（2026-09-25 上午开的，判据与连带成本都在里面）
1. **配对状态面四条小的**（全部有实测证据在 `docs/bugs/2026-09-25-link-lan-findings.md`，逐条判据如下）：
   - **(a) 归因写反**：票失效被拒时桌面面板记的是「对端已关闭连接」，真相是**桌面自己拒的**。
     先定位是谁覆盖的（怀疑是同一次失败之后那条半开连接的 EOF 把具体原因冲掉，别照猜改），
     再按「别让"谁先挂断"盖掉"谁拒的"」定口径 —— 与阶段 4 那条「附属失败别碰 `connected`」同族。
     判据：拿过期票配一次 → 面板那行必须是「配对码已过期或已用过，请重新打开」；
     随后接一条拨开就断的空连接（`nc host port` 即可）→ 那行**不许**被改成「对端已关闭连接」。
   - **(b) 手机在桌面点「允许」之前就报 `connected:true`**：TOFU 挂着没人理时手机显示已连接、
     每条 `link_request` 等满 30 s 才失败，桌面 60 s 后记「配对请求被拒绝或超时」——
     用户看到的是"扫完码手机说连上了，然后做什么都没反应，报错还都指向网络方向"。
     方向：桌面进入 `run_pump`（真正开始服务）时**立刻**发一个 Ping（现在要等一个 HEARTBEAT），
     手机把 `connected` 推迟到收到对端第一帧，中间那一档在界面上叫「等待电脑上确认」。
     **连带成本要说清**：`link-verify` 的 B/G 段里 Node 扮演桌面，握手完也得补那一下 Ping，
     否则应用当客户端永远不转已连接 —— 那是这段判据本来就该跟着协议走，不是给测试开后门。
     判据：发起配对后、允许前手机 `connected===false` 且界面有那一档；点允许 1 s 内转已连接；
     点拒绝则手机拿到「桌面端拒绝了这次配对」而不是超时。这条**必须用普通票**测（测试档会跳过 TOFU）。
   - **(c) 手机侧 `peerDevice` 恒空**：连上了也没有电脑名可显示（`peerKeyId` 有，免扫重连不受影响）。
     根因方向：`hello.device` 是**手机→桌面**那一个方向的名字，`Msg::Auth` 只带 id+sig，
     所以桌面名从没上过网。两条路选一条：Auth 载荷里带上 `name`，或手机把配对码 / 二维码里的
     `name` 字段存进 prefs 当显示名 —— 选**前者**，因为免扫重连那条路上没有配对码可查。
     判据：连上之后 `link_status.peerDevice` 非空，手机「已连接的设备」显示电脑名（两台真机各验一次）。
   - **(d) `toobig` 文案自相矛盾**：7,000,000 字节的文件报「这个文件 6 MB，超过远程打开上限 6 MB」，
     实测值与上限显示成同一个数，用户判断不出超了多少。给 6.7 MB 或「超出 0.7 MB」都行，
     但**只能有一份口径**：尺寸格式化别再写第二套（`fsrv.rs` 里那个整除就是病灶）。
   验收：`cargo test --lib link::` + `npx vitest run` + `link-verify` A–J 全绿 +
   跨机 `pairtest` / `protocol` 复跑；(b)(c) 还要一次**真 TOFU 流程**（关掉测试档）。

#### 留档：2026-09-25 这一批（`f014d98`）收口了什么

下面四条是上一轮的工单，原文留着，因为每条都带着"当时是怎么定的"的信息，别删。

1. **跨机局域网实测做完两轮，第二轮推翻了第一轮的结论：阶段 2/3/4 全部通过**，
   全量在 `docs/bugs/2026-09-25-link-lan-findings.md`。一句话版：阶段 2/3 逐条过
   （读写编码换行、冲突基线、逃逸四类、超限、标签同步、免扫重连零点击），
   §11.1 防火墙那条在另一台真机上证到「应用自己 bind 的端口入站可达」，
   **阶段 4 跨机 + 桌面本地根形态下 5/5 到帧、首帧 54–82 ms（中位 60 ms）**。
   ~~实时推送 5 次只到 1 次、怀疑共享根是网络路径~~ —— **两条一起作废**：判据脚本把
   `transformCallback(cb, true)` 的第二个参数当成了 persistent，它其实是 `once`
   （收到第一条后回调自己注销），于是「每次注册只到一帧」是工具的形状而不是产品的。
   定位靠三路独立对质：桌面侧 `[heid-fsDBG] 泵要发`、手机端 logcat 的 `RustStderr` 标签
   （10 帧全收到且 `app.emit` 全部成功）、页面里的监听计数（只有 1 条）。
   **教训同一条**：跨机观测的每条判据都要有第二个通道能对质，「该来的来了 + 不该来的没来」
   在工具自锁时会一起给出错答案（与阶段 4 收口记录里 `frameReader` 漏帧那次同一类）。
   只剩一种形态没量过：**共享根是 UNC 网络路径** —— 不再是发版阻塞，要量就把根换成
   `\\<这台机>\git\link-test` 复跑一次探测即可。
   复现工具已固化成 **`scripts/link-lan-check.mjs`**（`status` / `reconnect` / `fsprobe` / `protocol` /
   `pair` 五件事，只要一个能改的共享根加一条局域网链路，不需要真机）。
   **尺子修好之后 I 段稳定复现出一条真缺陷并已修掉**：`watch.rs::Batch::add` 只判祖先层的黑名单，
   而 Windows 会连「目录自身」一起通知，于是 `<根>\node_modules` 漏成一帧 `dirs:[""]` ——
   一次 `npm install` 仍然会涌进推送队列，正是那条黑名单本来要挡住的事。
   修法与"点文件不能顺手吃掉"的取舍记在 `docs/bugs/2026-09-25-link-lan-findings.md`。
   环境账也在那份文档里（AVD 每次开机要 `cmd wifi connect-network AndroidWifi open`、
   CDP 要用 `__TAURI_INTERNALS__`、模拟器页面时钟慢 38.7 小时别拿来量延迟）。
2. ~~**测试专用配对模式**~~ — **已做并实测（2026-09-25）**。三条守则都按原话来：
   **①** 显式开关：命令 `link_test_pair_set(on)`（四处登记齐全）+ 设置 →「设备互联」里带警告的一行，
   默认关、**且不落盘**（`LinkState::test_pair` 不进 link.json，重启必关）；
   **②** 开启态常驻可见：新组件 `LinkShareChip` 落在桌面状态栏（`测试配对开着 · <设备名>`，警示色），
   顺带补上阶段 1 定稿第 1 条一直欠着的那半 —— 「共享开着」这件事此前**只在设置页里看得见**，
   现在常态显示「手机可访问 · <根>」，tooltip 里带共享根与根外白名单份数；
   **③** 不跳审计：自动允许那次照样登记、照样 `persist`、照样把 `peerDevice` 报出来，
   并在 `PairedDevice.via_test` 上打标记，「已配对设备」列表里那几行带「测试配对进来的」。
   **做法上最值钱的一条**：没有新增协议面 —— 用的是一张**值公开的哨兵票**
   （`pair::TEST_TICKET`，32 位十六进制，与随机票同形状），所以手机侧扫码 / 粘贴 / 6 位短码
   三条入口**一行都没改**，旧版手机也照样能配；协议里既没有 `slot="test"` 也没有新命令。
   配套两条别漏：开着时 `link_pair_qr` 与 `spawn_server` 都**不换票**（点一次面板就把门关了是最难查的形状），
   关掉时把哨兵票摘掉（留着就是"设置里已经关了但还能连"）。
   固定短码 = 由那张票派生，**别把数字抄进文档当权威**：`link-lan-check.mjs` 与 J 段各自重算一遍，
   J 段再拿桌面上显示的码对一次，三处同源才说明派生规则没漂（本轮实测为 `163988`）。
   验收：`cargo test --lib link::` **97 例**（新增 4 例，含「测试票永不过期也用不完」与
   「普通票的用后即废没被这档带松」）+ `npx vitest run` **1084 例**（新增 `LinkShareChip` 6 例、
   `normalizeStatus.testPair` 2 例）+ `link-verify` 新 **J 段 10 项**（没开时配不上 → 开着免确认连上 →
   账上有 `viaTest` → 关掉立刻被拒 → 回到随机票）+ 真链路：`node scripts/link-lan-check.mjs pairtest`
   模拟器 1 s 连上、桌面零点击，随后 `protocol` 14/14、`fsprobe` 5/5。
   **跨机复跑也已成立**（2026-09-25，`.174` 上跑他自己编译的 exe、测试档在对面开着）：
   `pairtest` 1 s 连上零点击 → `protocol` **14/14** → `fsprobe 5 20 --file=nested/pair.json` **5/5**、
   首帧 53–69 ms（报的是 `dirs:["nested"]`，子目录那一层也对）。
   对面显示的短码与这台机算出的 `163988` 一致，顺带成了一次跨构建的 `TEST_TICKET` 自检。
   以后跨机实测的现场就三步：桌面开共享 → 开测试配对 → 手机 `pairtest`，不需要任何人守在电脑前点允许。
   **仍然欠着、没挤进这批的两条**（同属配对状态面，但要动的地方不一样）——
   **已排进上面第 1 条的 (a)(b)**，判据与连带成本在那儿：
   (a) 票失效被拒时桌面面板记成「对端已关闭连接」—— 要改 `serve_conn` 的错误归因路径；
   (b) 手机在桌面点「允许」**之前**就报 `connected:true`（TOFU 挂着没人理时，手机显示已连接、
   每条 `link_request` 等满 30 s 才失败，桌面 60 s 后记「配对请求被拒绝或超时」）——
   要动两端连接状态机，还得连带改 `link-verify` 的 B/G 段（Node 当桌面时也得发那一下"我开始服务你了"），
   批起来风险更大，所以单独一轮。
3. **阶段 5 开工前先读阶段 4 的这条**（**已并入上面新工单第 2 条**，原文留着）：离线队列的"恢复后回写"用的就是 `write` 的基线哈希与
   `conflict` 回传，本轮的 `fs` 推送只是**提示**，权威永远是内容哈希 —— 别把"收到帧"当成"可以安全覆盖"。
   工单原文：拔网线编辑不丢，恢复后回写或明确请确认；**真关 WiFi**，不用代码模拟；进程被杀用
   「设置里强行停止」再开。条目 44 是 v1.5 里风险最高的一项，独立成轮，别拖住前面已经成立的主题。
4. ~~v1.5.0 还欠 51（语言高亮覆盖）与 52②（弹窗层 48dp 提档）~~ — **两笔都已做**（2026-09-25，`d5c7e47`）：
   语言补到实测 31 个键、安装包增量 +40.3 KB gzip；弹窗层 44 档清零并两台壳 dump rect 实测。




42. **链路基座与扫码配对**
    - 帧协议：`u32` 大端长度前缀 + JSON，双向（请求/响应 + 服务端主动推送）；心跳与断连判定；
      `std::net` 直接承载（**零新增网络框架依赖**），阻塞调用一律 `spawn_blocking`（照 `http.rs:95` 的既有写法）
    - **传输加密前置到本阶段，不留后补**：XChaCha20-Poly1305 + 配对票经 HKDF 派生密钥
      （事后加加密要动整个帧协议与所有命令的编解码路径）。局域网不等于可信网络——公司网、
      咖啡店 WiFi、宿舍都能嗅探
    - 扫码配对：桌面生成二维码、手机应用内扫码（系统相机不认自定义 scheme，故必须应用内做，
      需 `CAMERA` 权限 + 懒加载解码 chunk，不进主包）、TOFU 确认框、记住设备免重复扫码；
      手输 6 位配对码与手填 IP 双兜底
      —— **2026-09-23 阶段 1 落地**：配对票（一次性 2 分钟、用后即废）、二维码带桌面长期身份指纹
      （防同网段抢答）、TOFU 确认、配对后双方各存一把从已认证 ECDH 派生的 LS、免扫自动重连、多配对
      单在线、6 位短码与手填 IP 兜底，全部实现并 `link-verify` 端到端实测通过；**唯独手机应用内
      相机扫码器押后**（等 §7.2 三点否决性实测），现在手机侧走「粘贴 hide-link 配对码」这条等价入口
    - `hello` 带协议版本并强校验：安卓侧载无静默更新，两端版本会长期不一致
    - 服务端**不重写文件逻辑**：`read` 复用 `encoding::detect_and_decode`、`write` 复用既有写盘命令、
      `stat.size` 复用 `large_file::file_size`。手机拿到的编码 / BOM / 换行 / 二进制判定与桌面逐字一致，
      不必退回 `fileIO.ts:144-147` 那条 JS 启发式检测的降级路径
43. **远程文件、当前标签与实时同步**
    - 共享范围 = 桌面文件树当前根（`fileTree.ts:297` 的 `heid-tree-root`），**不额外再选一次**；
      根外已打开的文件逐个进白名单（只允许具体文件的规范化路径，不给目录、不给通配、不给前缀匹配；
      每次读写重新 `canonicalize` 校验；关标签即移除）
    - **路径逃逸防护测试先行**（`..` 穿越 / Windows 盘符切换 / UNC / symlink 外指）——
      这是本版唯一「写错就等于把整个磁盘暴露给局域网」的地方，与 v2.0 条目 46 的 fs 权限收窄同向，
      可视为其前置工作
    - `hide-remote://<deviceId>/<percent-encoded 相对路径>` 作为身份键（用 deviceId 而非 IP，
      换网 / DHCP 重分配后已打开的标签仍指向同一台设备），接入 `platform.ts` / `fileIO.ts` /
      `largeFile.ts` 的既有平台分派点；`remoteDirLister` 作为 `DirLister` 的第三实现（`fileTree.ts:201-293`）
    - 桌面「当前聚焦窗口」的标签页同步（服务端进程级单例，各窗口上报标签到 Rust `State`，
      按 Tauri 焦点事件取聚焦窗口那份）。**脏标签不跨设备接管**——两端永远只有一份「正在改的内容」，
      杜绝「说不清哪边是对的」；无路径标签走移交语义（见条目 44）
    - 实时更新：`watchImmediate` 递归监听（**不可用 `watch()`**：其 JS 封装硬编码注入 `delayMs=2000`
      走 notify-debouncer-full，7.7 万文件目录实测冻结 4.7~8.5s，见 `fileTree.ts:273-281` 的诊断注释）
      + 复用 `search.rs:227` 的 `is_skipped_dir` 与 symlink 跳过（否则一次 `npm install` 就能刷爆链路）
      + 300ms 去抖（与 `useExternalFileWatcher.ts:27` 同值）+ 长连接推送。无已配对设备在线时不建监听
    - 保存冲突与「桌面已更新」一律走**既有 diff 时间线**逐条采纳，不做自动三方合并
    - 能力边界如实标注：大文件（>32MB）不远程打开（`large_file` 分块读取是 `#[cfg(desktop)]`）、
      跨文件搜索不含远程、手机端系统级操作（在资源管理器中显示 / 复制 / 剪切）置灰
44. **离线暂存与未保存内容移交** —— **前半（离线队列，阶段 5）已完成并实测 2026-09-25**，
    后半（跨设备移交，阶段 6）未动。交付口径与按实测改掉的三条见「阶段 5 收口记录」。
    - 离线写队列存 **IndexedDB**（不用 localStorage：它有 5MB 量级悬崖，而这里承载的是
      「用户以为已经存好了」这份信任；项目内无先例，需自建极薄封装 + 测试）。
      进程被杀与应用重启后仍存活；同一文件多次离线编辑**折叠为一条**——内容取最新、
      `baseHash`/`baseMtimeMs` 保留**最初读到时的基线**，否则回放时会拿中间态比对产生假冲突
    - 文件树五态标记（已同步 / 待同步 / 冲突 / 桌面已更新 / 离线），沿用既有小圆点视觉语言
      （`FileTreeSidebar.tsx:646` 的 `bg-amber-500`），中英双语 + 触屏长按菜单等价物
    - 重连按 `queuedAt` 顺序回放：基线一致则静默写回；**桌面期间也改过则标红请用户裁决**
      ——两边都是有效修改，程序替用户选哪边都可能毁掉工作，这是问题本身的性质而非实现不到位。
      文案写「有 N 处需要你确认」而不是「同步失败」
    - 写满或写入失败**明确告警**，不得沿用 `drafts.ts:71` 的静默跳过（超限即 `return false`）；
      告警要说清「这一份没能存进手机，请复制到别处」
    - 未保存内容跨设备移交：`SessionTab` 增 `transferred` 变体（`session.ts:15-17`），
      复用多窗口 bootstrap 已验证的「传 `SessionState` → 走 `restoreSessionTabs`」通路；
      光标位置用既有 `jumpRequest`（`tabModel.ts:64-66`）承载；单条上限沿用 `MAX_DRAFT_CHARS`
    - 本版风险最高的一项，测试密度要求高于其余：进程被杀、存储写满、回放途中再次断连、
      同文件并发编辑、基线折叠逻辑，都要有测试

51. **语言高亮覆盖**（自 v1.4.2 移入本版，执行口径见下；现状描述仍在 v1.4.2 那条）
    - 分三档补，逐档落地逐档实测，不一次全加：**①** 官方包 —— Go（`@codemirror/lang-go`；
      C/C++ 已由 `lang-cpp` 覆盖，C# **没有**官方包）；**②** `@codemirror/legacy-modes` 的
      `StreamLanguage` —— Shell / INI / Dockerfile / Makefile（同一套 stream 基座，一次引入四种），
      TOML 走社区包；**③** 社区包（C# / Ruby / Swift / Kotlin / Scala / SCSS / Less）——
      **先查维护状态再决定，不为一句话引入无人维护的包**
    - **体积口径要勘正**：`LANGUAGE_LOADERS`（`src/lib/codemirror.ts:11-33`，实测 18 个键）全是动态
      `import()` 的本地 chunk，而本地 chunk 是**打进安装包**的 —— 懒加载只减首屏，**不减 APK / NSIS 体积**。
      所以原句「语言包都是懒加载 chunk，不影响主包体积」对离线分发的 H.I.D.E 是误导，
      本条的门禁应写成安装包增量（建议上限 150 KB），不是主包 gzip
    - **加载失败在界面上完全看不出来**：`loadLanguageExtension` 的 catch 分支只 `console.warn`
      后返回 `null`（`:44-51`），表现就是"按纯文本渲染"，与"这个语言本来就没着色器"逐字相同。
      所以每种新语言都要有一条断言"扩展确实拿到了非 null"的测试，不能靠看截图收口
    - 补到哪一步才允许把「30+」写回 README：按 ①②③ 实际落地的种数算，并重新实测
      `LANGUAGE_LOADERS` 的键数，别再写估计数
    - **✅ 2026-09-25 落地：13 种全补上，`LANGUAGE_LOADERS` 实测 31 个键**（README 两处按 31 改写）
    - **原计划的三档分配不成立，按实际依赖重分了**：`@codemirror/legacy-modes@6.5.4`（官方、
      2026-09-02 仍在更新）一家覆盖 **10 种** —— shell / dockerfile / toml / ruby / swift /
      clike(C#、Kotlin、Scala) / css(SCSS、Less)。**它没有 ini 也没有 makefile**，
      所以「② 走 legacy-modes 的 INI / Makefile」这句是当初没核实就写下的。
      npm 上唯一的 `codemirror-lang-makefile` 是 0.1.1、最后发布停在 2024-06、单一维护人 →
      按本条门禁「不引入无人维护的包」不取。两者改为仓库内自维护 `src/lib/streamLangs.ts`。
      社区包一个都没引：③ 整档被 legacy-modes 吃掉了。Go 走官方 `@codemirror/lang-go@6.0.1`
    - **安装包增量实测**（口径就是本条要求的「增量」而不是主包 gzip）：把 `codemirror.ts` 临时退回
      HEAD 版另建一份 `dist-baseline` 对比 → 新增 10 个 chunk，assets 合计
      11404.5 → 11509.5 KB raw（**+105 KB**）、4004.0 → 4044.3 KB gzip（**+40.3 KB**），
      150 KB 门禁内；首屏（`index.html` 实际引用的 1 JS + 1 CSS）830.7 → 834.0 KB gzip
      （+3.3 KB，是 `StreamLanguage` 进了主包）。README 顺带勘正：首屏 814 → 834 KB、chunk 284 → 296
    - **测试口径**（`src/lib/codemirror.test.ts`，7 例）：不止断言非 null —— 逐语言拿一段样例过
      真实解析管线，用**应用自己的配色主题**取 class，断言「注释 / 关键字 / 字符串 / 数字」这些
      不同类别各自着色且互不同色（同色等于没区分，与纯文本无异）；外加断言没有
      `Unknown highlighting tag` 警告。README 的语言数由测试守住：写了别的数字就红
    - **两条会反复踩的实现账**：**①** stream 着色器的 token 名直接映射 `@lezer/highlight` 的 tag，
      `definition` / `function` 这类**是修饰函数不是标签**——裸用不报错，节点直接从语法树里消失，
      表现和「这个 token 压根没着色」逐字相同；要修饰语义只能用传统别名 `def`（= `variableName.definition`）。
      **②** `simpleMode` **行首不重置状态**（它没有 sol 钩子），任何 `push` 都会漏到下一行 ——
      要逐行判定状态的语法（Makefile 的 Tab 配方行）不能用它，得写手工 `StreamParser`
    - **如实记下的短板**：legacy-modes 的 dockerfile 只给注释与指令两类 token，参数不着色；
      Makefile 配方行的命令原文不着色（只认 `$(...)` / 引号串 / 行尾注释）。都是上游语法的天花板，
      不是接错线

52. **② 弹窗层 48dp 提档**（自 v1.4.2 移入）
    - 清单：设置 / 关于 / Diff / 图片插入 / 确认框、CSV 网格菜单、Markdown 表格结构条，
      外加 v1.4.2 条目 49 的「获取另一版」面板按钮（当时按现有 44dp 档写的，一并抬）
    - 与互联的搭车点：阶段 1 的配对确认框、阶段 5 的五态长按菜单都是新写的手机/平板界面，同一轮量。
      但**不要绑成同一个提交** —— 提档动的是既有弹窗的测试断言，与互联功能互不相干，混在一起
      出问题时两头都查不清
    - 量的方法：`touch-target-audit` 在本应用报的约 60 条子 48dp 项绝大多数是噪声（标签内 `svg`、
      脏点、`cm-gutterElement` 行），**按改动面单独 dump rect 量**，不要引用它的总数
    - **✅ 2026-09-25 落地并模拟器实测**。做法与结论：
    - **范围按「44 档清零」定，不止清单那八处**：静态扫出的 44 档字面量（`min-h-[44px]` /
      `min-w-[44px]` / `pointer-coarse:h-11|w-11` / `TOUCH ? 'w-11'`）全仓一次抬到 48，
      连带 SVG 检查器·画布·工作台、ImageViewer、UrlImportModal、MermaidEditModal、
      DeviceLinkSection 漏网的一处、QrScanner 的 44×44 圆按钮。留一半改一半会造出
      「设置 48 / SVG 面板 44」两套档——53 那颗点就是这么来的。
      守卫：`src/lib/touchSize.test.ts` 静态扫源码，44 档再出现就红
    - **量的工具新写了 `scripts/popup-touch-audit.mjs`**：连 adb 转发出来的 WebView 调试端口，
      只量**当前这一屏**实际会点到的控件，低于 48dp 列出来并以退出码 1 收口；
      噪声按 52 的既有判断滤掉（标签条 `[data-tab-id]`、编辑器内部、拖拽把手）。
      状态切换用 `scripts/cdp-audit.mjs` 点界面，文件用 MediaStore `content://` 走 VIEW intent 打开
    - **实测（x86_64 debug 包 / 平板壳 2560×1600 @320dpi = 1280 CSS px / dpr 2）—— 六处违例全靠 dump rect 才现形，静态数 class 数不出来**：
      CSV 编辑栏容器写 `min-h-[48px]` 但带 1px `border-b`，`items-stretch` 的子里只剩 **47dp**
      （下限要写在各控件上，不是写在行上）；CSV 行筛选输入 112×**20dp**；Diff 页脚 ± 按钮 24×24、
      接受/撤销 67×28 与 91×28；关于弹窗的关闭 × 22×22；设置·设备互联「高级：手填 32 位配对码」
      的 `<summary>` 718×**16dp**；取色器色相/不透明度轨道 242×**28dp**（改成 48dp 热区靠
      `h-12 + py-2.5 + bg-clip-content`，视觉粗细保持原样）。
      复测：CSV 网格、设置（含「高级配对码」展开）、关于 + 获取另一版（点「打开下载页」后的面板）、
      Diff（含页脚接受/撤销与 ±）、网址导入、SVG 工作台、取色器各屏「最小边 ≥48dp」全绿。
      **两层这次没能开到位、只改了 class 没上机量**：确认框（Diff 的撤销确认要先有时间线条目才启用，
      当前装机包的时间线是空的）与图片插入弹窗（平板壳上没找到「插入图片」入口，它写在移动端顶栏）。
      两者的按钮与已量到的 AlertDialog / ImageInsertModal 底部按钮是同一套写法
    - **两台壳都量了**（前几批的口径）：平板壳 1280×800 CSS px（dpr 2）与手机壳 412×732（dpr 2.625，
      `phone35`）。手机壳上重点量了「关于」——它本来就只剩十几像素余量（v1.4.2 加 max-h + overflow-y-auto
      的原因），抬档后实测面板高 708、上下各留 12px、内部无裁切 ✅；CSV 网格 / 设置 / 高级配对码
      在手机壳同样 ≥48dp
    - 顺带把「弹窗不得溢出可视框」并进 `popup-touch-audit.mjs` 的判据。写第一版时把越界量算成了
      `max(0, top)`（等于把面板离顶多远当成越界），取色器被误报「溢出 196dp」——
      正确是 `max(0, -top)`。**新加的门禁自己也要先验一遍再信**
    - **量出来但本条不动的，另记**：markdown 大纲栏「展开大纲」把手 20×44dp、
      `App.tsx` 侧栏收合把手 `h-11 w-5`（44 高但只有 20dp 宽）——都在主界面不在弹窗层；
      标签条（44 高、关闭 × 28×28）维持 v1.4 的原判断不做


59. **安卓 release 包上，「网址导入」会把加载错误页当正文导入**（2026-09-23 做 v1.5 前置实测时撞出来的，
    与本版主题无关，随 v1.5.0 一起修）
    - 现象：导入一个 `http://` 明文网址，**Rust 侧静态抓取其实已经成功**（本机服务端确实收到
      `ua="ureq/2.12.1"` 的 200 响应），但最终打开的标签页叫「网页无法打开.md」，正文是浏览器的
      加载失败页：`net::ERR_CLEARTEXT_NOT_PERMITTED`
    - 根因是两层叠加：**①** 正文过薄触发了「内置浏览器渲染」第二层兜底，而安卓那条走
      `HeidBridge.renderPage` 的**离屏 WebView**（`src/lib/renderFetch.ts:1-8`），受
      `usesCleartextTraffic="false"` 约束 → 加载失败；**②** 失败没有以「失败」回传，
      而是把 `chrome-error://chromewebdata/` 这个错误页当渲染结果返回，`importFromFetched`
      的择优又因为它比静态正文"更厚"而选了它 —— 用户于是看到错误页而不是已经抓到的内容
    - 注释里写的是「任何失败都静默返回 null，导入永不因兜底失败而中断」，**实际没做到**：
      错误页有可见文本，覆盖率判据（`urlImport.ts:297` 的 `MIN_COVERAGE`）拦不住它
    - 修法方向：渲染结果带 `finalUrl`，**`chrome-error://` 前缀直接判为失败返回 null**（信号现成，
      本次的错误页正文里就有这一行）；若静态正文同样过薄，就如实说「该页需 JS 渲染，
      安卓端渲染不支持明文 http」，别拿错误页冒充正文
    - 影响面如实界定：**只影响安卓 release + `http://` + 静态正文过薄**这一组合；
      `https://` 与桌面端都不走这条路。但 `http://` 是内网 / 本机调试地址的常态写法，不算边角


## v2.0 —— 可选 AI 与安全加固（P2）

主题：**在不破坏"秒开、2-3 MB"的前提下，给一个默认关闭的 AI 入口**。

45. **AI 按需接入（默认关闭）**
    - 形态约束：不常驻面板、不自动改文件；只能由快捷键 / 命令显式触发**单次**请求
    - 接入方式：优先对接 OpenAI 兼容端点（Ollama 本地端点即可跑通），把「离线、零费用、内容不出本机」作为默认推荐
    - 结果处理：生成内容先落在标签页或 diff 里，由用户逐条采纳 —— 复用既有 diff 时间线，规避「AI 改坏文件」的不信任
46. **安全加固**
    - `tauri.conf.json` 的 `csp: null` 收紧为白名单策略
    - `src-tauri/capabilities/default.json` 的 fs 权限从 `path: "**"` 收窄到用户实际选择的目录
      （v1.5 条目 43 的路径逃逸防护已把「按用户选定的根收窄」这件事在远程通道上做过一遍，可直接沿用其思路）
    - 更新链路：已用 minisign 签名，参照 Notepad++ 因更新器未校验下载物而遭供应链投毒的事件（CVE-2025-15556），补上「校验失败即中止」的显式分支与版本回滚说明
47. **工程化收口**：引入 lint / format 并接入 CI；为 19 个 hooks 中尚无测试的 16 个补关键路径测试，
    并为 `App.tsx`（2,796 行，全仓最大且零测试）建立第一条测试；
    `App.tsx` / `MarkdownPreview.tsx`（1,979 行）/ `CodeEditor.tsx`（1,871 行）/ `CsvGridEditor.tsx`（1,586 行）继续拆分

---

## 明确不做（守住"轻量"）

插件系统、LSP/智能补全、内嵌终端、Git 集成、账号与云同步、主题市场、AI 面板常驻、
电子表格能力（公式 / 多 sheet / 单元格样式）、跨文件批量替换与宏录制。
—— 需要这些的用户有 VS Code；H.I.D.E 的目标是"秒开、约 7 MB、够用的编辑器"。

关于「账号与云同步」的边界（v1.5 之后需要讲清，否则看着自相矛盾）：**不做的是账号体系与云中继**，
v1.5 的设备互联是**局域网内两台设备直连**——无账号、无服务器、内容不出局域网、开关关闭即完全释放端口。
两者不是一回事。设备互联自己的「明确不做」清单（实时协同编辑 / CRDT、跨公网穿透、手机当服务端、
自动三方合并等）见设计稿第 15 节。

另：以下两条经查证属于**有意妥协**而非待补的缺口，不要再当遗留项重提——

- **平板标签关闭钮维持 28×28**：标签内扩到 48dp 会压到邻标签，替代出口是长按标签弹菜单。
- **实时协同编辑 / 两端同时持有同一份未保存内容**：会产生「说不清哪边是对的」的状态，
  v1.5 的语义定为「移交」（所有权转移）而非「同步」。

## 优先级依据

1. **打开文件这条路径优先**：大文件、乱码、换行符、找不到内容 —— 这四项决定用户第一分钟的去留，v1.2 全部收口。
2. **再补桌面顺手度**：跨文件搜索、打印 / 导出 PDF、十六进制查看，都是"平时不用、要用时必须有"的能力，且都不需要常驻进程。
3. **然后是内容格式深度**：Markdown / CSV / SVG / 图片已经把优势立起来，JSON / YAML 与打印是顺着同一逻辑的延长线。
4. **多平台与 AI 放最后**：两者都会显著增加维护面或包体积，且都不是当前用户停留的原因；先给结论（做 / 不做），再谈实现。
   —— 多平台的结论已在 v1.4.1 条目 40 给出：**不做**，Windows + 安卓单平台做深（依据见该条）。AI 仍留在 v2.0。
5. **设备互联排在触屏收尾之后、AI 之前**：它解决的是「同一个人在两台设备上被割裂」这个真实痛点，
   比 AI 更贴近编辑器本职；且它**大量复用既有机制**（`DirLister` 抽象、`SessionState` 载荷通路、
   `jumpRequest`、diff 时间线、递归监听、编码检测链），新增依赖极少，与「轻量」定位不冲突。
   但它引入网络与并发状态，维护面显著大于此前任何一版，故单独成一版、不与其他主题混挂。
